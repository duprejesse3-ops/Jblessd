// GENERATED FILE — DO NOT EDIT BY HAND.
//
// Produced by packages/invoice-chaser/tools/embed-source.mjs from the real
// package source. Regenerate after changing the package:
//
//   node packages/invoice-chaser/tools/embed-source.mjs
//
// This is the payload for the Overdue Invoice Chaser product: the
// complete, runnable source the buyer receives at checkout. It is embedded
// rather than read from disk so fulfilment cannot fail on a missing file.

export interface SourceFile {
  path: string
  contents: string
}

export const INVOICE_CHASER_SOURCE: SourceFile[] = [
  {
    path: "README.md",
    contents: "# Overdue Invoice Chaser\n\nPulls your real open invoices straight from Stripe, works out how many days\noverdue each one is, and emails escalating reminders — friendly, firmer,\nfinal — at the thresholds you set, automatically. Not a Make/Zapier\nblueprint you assemble from scratch — a working script that does the whole\njob the moment it's configured.\n\n## How this differs from a reminder *checklist*\n\nA checklist tells a human to go check Stripe and chase whoever's late. This\nproduct does the chasing itself: it reads your actual open invoices, does\nthe days-overdue math, picks the right tone for how late each one is, and\nsends the email — every day, without anyone remembering to do it. It also\nkeeps track of what it's already sent, so a customer 12 days late doesn't\nget the \"day 3\" and \"day 7\" and \"day 14\" emails all at once, or the same\nreminder twice.\n\n## What it does, precisely\n\n1. On a schedule (default: once daily — edit the cron in the workflow), it\n   reads `config.json`: the escalation ladder — how many days overdue an\n   invoice has to be before it gets a reminder, and what tone that reminder\n   takes (e.g. day 3 = friendly, day 7 = firmer, day 14 = final notice).\n2. It pulls every open invoice directly from Stripe's Invoices API,\n   paginating through all of them, and skips anything with no due date set\n   (nothing to chase there).\n3. For each invoice, it computes exactly how many days overdue it is.\n4. It checks `state.json` for which thresholds have already fired a\n   reminder for that specific invoice, and sends a reminder only for the\n   **highest threshold newly crossed** — so an invoice you haven't looked at\n   in three weeks gets one email at the appropriate urgency, not a flood of\n   catch-up emails for every threshold it passed along the way.\n5. The reminder — a real HTML + plain-text email, sent via Resend — names\n   the invoice, the amount, the due date, how many days overdue it is, and\n   links straight to Stripe's hosted invoice page to pay.\n6. `state.json` is updated so that threshold never fires again for this\n   invoice, and pruned of invoices that are no longer open (paid, voided,\n   or deleted) so it doesn't grow forever.\n7. Optionally posts a one-line run digest to Slack: how many invoices are\n   overdue, how many reminders went out, and the total dollar amount still\n   outstanding.\n\nIt does not collect payment, adjust an invoice, or take any action inside\nStripe — it only reads open invoices and emails about them. You set the\nescalation ladder once, and it acts on exactly that from then on.\n\n## Install (10 minutes)\n\n1. Copy `bin/`, `lib/`, and `config.example.json` into your repo — anywhere,\n   e.g. `tools/invoice-chaser/`.\n2. Copy `config.example.json` to `config.json` in that same folder and edit\n   it: your own escalation days and reminder labels.\n3. Copy `.github-workflow-template/invoice-chaser.yml` to\n   `.github/workflows/invoice-chaser.yml` (this exact path — GitHub only\n   runs workflows from there).\n4. If you put `bin/`/`lib/` somewhere other than `tools/invoice-chaser/`,\n   edit the `working-directory:` lines in the workflow to match.\n5. Add repo secrets (**Settings → Secrets and variables → Actions**):\n   - `STRIPE_SECRET_KEY` — from a restricted Stripe key with **read** access\n     to Invoices (dashboard.stripe.com/apikeys)\n   - `RESEND_API_KEY` — from resend.com. **Leave unset to dry-run**:\n     reminders are still computed and logged every run, just never sent,\n     and `state.json` is not updated for anything that wasn't actually\n     sent — a safe way to watch it for a few cycles before it emails a\n     real customer.\n   - `CHASER_FROM_EMAIL` — a verified sending address on your Resend domain\n   - `SLACK_WEBHOOK_URL` (optional) — Slack → Apps → Incoming Webhooks\n   - `STORE_NAME` (optional) — appears in the reminder subject/body\n6. Commit and push. It runs on the schedule automatically, or trigger it\n   once immediately from the **Actions** tab (`workflow_dispatch`) to check\n   it actually works before waiting for the cron. The included workflow\n   commits the updated `state.json` back to the repo after each run so\n   dedupe state survives between runs — see the comment at the top of the\n   workflow file if you'd rather store it another way.\n\n## Testing without waiting for Actions\n\n```sh\nnpm install --omit=dev\ncp .env.example .env    # fill in STRIPE_SECRET_KEY, RESEND_API_KEY, etc.\ncp config.example.json config.json   # edit with your real escalation ladder\nnode bin/chase.mjs\n```\n\nLeave `RESEND_API_KEY` blank in `.env` for your first run — you'll see\nexactly which reminders *would* go out and to whom, logged to the console,\nwith nothing actually sent and no `state.json` entries written for them.\n\n```sh\nnpm test\n```\nRuns without any real API keys or network calls — checks the days-overdue\nmath, the escalation-threshold selection (including the catch-up/no-flood\nbehavior), the dedupe state tracking, the email content builder, and config\nvalidation against known inputs, not a live Stripe or Resend call.\n\n## On state.json\n\nThis product tracks, per invoice, which escalation thresholds have already\nsent a reminder — that's the entire job of `state.json`, and it's the only\nstate this product keeps. It's a plain JSON file, not a database, so you can\nread it, edit it, or delete an invoice's entry by hand if you ever need to\nforce a reminder to re-fire. If `state.json` goes missing entirely (a fresh\ncheckout, a wiped cache), the next run treats every invoice as never having\nbeen reminded — it will re-send the appropriate threshold for anything\ncurrently overdue, once.\n\n## License\n\nSee `LICENSE.md`. One-time purchase, for your own use — run it against\nunlimited Stripe accounts and invoices, not for resale as a standalone\nproduct.\n",
  },
  {
    path: "LICENSE.md",
    contents: "# License\n\n**invoice-chaser — perpetual single-purchase license**\n\n> This is a plain-language commercial license template. It has not been reviewed\n> by a lawyer. Have one look at it before you sell against it, and replace\n> `[SELLER]` and `[JURISDICTION]` with your details.\n\n## The short version\n\nYou bought it once. You own your copy forever. Run it against as many of your\n**own** Stripe accounts, invoices, and customers as you like. Do not resell it\nas a product of its own.\n\n## What you may do\n\n- Use the software for any purpose, commercial or personal, forever.\n- Run it on unlimited Stripe accounts, invoices, and customers that you own or\n  operate.\n- Use it in client work, and use the reminder emails it sends however you\n  like.\n- Modify the source freely. It is plain JavaScript with no build step precisely so\n  that you can.\n- Keep using it indefinitely. There is no license key, no activation, no expiry,\n  no phone-home, and nothing that stops working if [SELLER] does.\n- Keep and use any version you have received, forever, regardless of what happens\n  to later versions or to [SELLER].\n\n## What you may not do\n\n- Resell, relicense, sublicense or redistribute the software itself, in whole or\n  in substantial part, as a product or as part of a product whose value is\n  substantially this software.\n- Publish the source publicly, or include it in a public repository, package\n  registry, or template that others can obtain without buying it.\n- Remove or alter this license file or the attribution in the source headers.\n\nSelling the *output* — your own collections process, consulting on accounts\nreceivable — is explicitly allowed. Selling *the tool* is not.\n\n## Updates\n\nAny updates published within twelve months of your purchase are included at no\nextra cost. After that, your existing copy keeps working forever; new versions may\nrequire a new purchase. There is no subscription and no recurring charge of any\nkind.\n\n## Refunds\n\nBecause this is source code and delivery is immediate, a refund is available\nwithin 14 days of purchase if the software does not work as described. Run\n`npm test` before you ask — it takes a second and tells you whether the software\nis at fault.\n\n## Warranty and liability\n\nThe software is provided \"as is\", without warranty of any kind, express or\nimplied, including but not limited to the warranties of merchantability, fitness\nfor a particular purpose and non-infringement.\n\nIn no event shall [SELLER] be liable for any claim, damages or other liability,\nwhether in an action of contract, tort or otherwise, arising from, out of or in\nconnection with the software or its use.\n\nIn particular: this tool reads your real Stripe invoice data and sends real\nreminder emails to the customer addresses on those invoices. It does not\ncollect payment, negotiate terms, or take any action inside Stripe itself —\nit only reads open invoices and emails about them — and it does not\nguarantee any invoice actually gets paid or that a reminder email is\nreceived; a customer's spam filter, an incorrect email on the invoice, and\ntheir willingness to pay are all outside this tool's control. You are\nresponsible for your own API keys, tokens, and their associated costs and\naccess scopes, and for reviewing the escalation wording against your own\ncollections policy before it reaches a customer.\n\n## Governing law\n\nThis license is governed by the laws of [JURISDICTION].\n\n---\n\nCopyright © 2026 [SELLER]. All rights reserved.\n\nThe source files each carry the same notice. Copyright in this software arises\nautomatically on creation and is not conditional on registration, on this notice,\nor on any filing — the notice exists to make ownership unambiguous and to travel\nwith a file that gets separated from this license.\n",
  },
  {
    path: "package.json",
    contents: "{\n  \"name\": \"invoice-chaser\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Pulls your real open Stripe invoices, computes how overdue each one is, and emails escalating reminders at the thresholds you set — not a blueprint you wire yourself.\",\n  \"license\": \"SEE LICENSE IN LICENSE.md\",\n  \"type\": \"module\",\n  \"engines\": {\n    \"node\": \">=18\"\n  },\n  \"bin\": {\n    \"invoice-chaser\": \"./bin/chase.mjs\"\n  },\n  \"main\": \"./lib/config.mjs\",\n  \"files\": [\n    \"bin\",\n    \"lib\",\n    \".github-workflow-template\",\n    \"config.example.json\",\n    \"README.md\",\n    \"LICENSE.md\"\n  ],\n  \"scripts\": {\n    \"chase\": \"node bin/chase.mjs\",\n    \"test\": \"node test/run.mjs\"\n  },\n  \"dependencies\": {},\n  \"devDependencies\": {}\n}\n",
  },
  {
    path: ".env.example",
    contents: "# Copy to .env for local testing. Inside GitHub Actions these are repo\n# secrets, not a file — see .github-workflow-template/invoice-chaser.yml.\n\n# Stripe (https://dashboard.stripe.com/apikeys) — a restricted key with read\n# access to Invoices is enough; this product never writes to Stripe.\nSTRIPE_SECRET_KEY=\n\n# Resend (https://resend.com) — used to actually send reminder emails. Leave\n# unset to dry-run: reminders are still computed and logged, just not sent,\n# and the dedupe state file is NOT updated for a threshold that wasn't sent.\nRESEND_API_KEY=\nCHASER_FROM_EMAIL=billing@yourdomain.com\nSTORE_NAME=Your Company Name\n\n# Optional — posts a one-line run digest to Slack (overdue count, reminders\n# sent, total $ outstanding).\nSLACK_WEBHOOK_URL=\n\n# Optional — override the default ./config.json and ./state.json paths.\nCHASER_CONFIG_PATH=\nCHASER_STATE_PATH=\n",
  },
  {
    path: "config.example.json",
    contents: "[\n  { \"days\": 3, \"label\": \"Friendly reminder\", \"tone\": \"friendly\" },\n  { \"days\": 7, \"label\": \"Firmer reminder\", \"tone\": \"firm\" },\n  { \"days\": 14, \"label\": \"Final notice\", \"tone\": \"final\" }\n]\n",
  },
  {
    path: "lib/config.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nimport { readFileSync } from 'node:fs'\n\n/**\n * Loads and validates config.json — the escalation ladder: how many days\n * overdue an invoice has to be before it gets a reminder at that tone, and\n * what to call it. Kept as plain JSON (not code) so a non-developer on the\n * team can adjust the schedule without touching the script.\n *\n * @param {string} path\n * @returns {Array<{days:number,label:string,tone?:string}>} sorted ascending by days\n */\nexport function loadConfig(path) {\n  let raw\n  try {\n    raw = readFileSync(path, 'utf8')\n  } catch (err) {\n    throw new Error(`Could not read config file at \"${path}\": ${err.message}`)\n  }\n\n  let parsed\n  try {\n    parsed = JSON.parse(raw)\n  } catch (err) {\n    throw new Error(`\"${path}\" is not valid JSON: ${err.message}`)\n  }\n\n  if (!Array.isArray(parsed)) throw new Error(`\"${path}\" must be a JSON array of escalation thresholds.`)\n  if (parsed.length === 0) throw new Error(`\"${path}\" must define at least one escalation threshold.`)\n\n  const thresholds = parsed.map((line, i) => validateLine(line, i, path))\n\n  const seen = new Set()\n  for (const t of thresholds) {\n    if (seen.has(t.days)) throw new Error(`\"${path}\" has more than one threshold for day ${t.days}.`)\n    seen.add(t.days)\n  }\n\n  return thresholds.slice().sort((a, b) => a.days - b.days)\n}\n\nfunction validateLine(line, i, path) {\n  const required = ['days', 'label']\n  for (const field of required) {\n    if (line[field] === undefined || line[field] === null || line[field] === '') {\n      throw new Error(`\"${path}\" entry ${i} is missing required field \"${field}\".`)\n    }\n  }\n  if (typeof line.days !== 'number' || !Number.isInteger(line.days) || line.days <= 0) {\n    throw new Error(`\"${path}\" entry ${i} (${line.label}): \"days\" must be a positive integer.`)\n  }\n  if (typeof line.label !== 'string' || !line.label.trim()) {\n    throw new Error(`\"${path}\" entry ${i}: \"label\" must be a non-empty string.`)\n  }\n  return { days: line.days, label: line.label, tone: line.tone ?? 'neutral' }\n}\n",
  },
  {
    path: "lib/mailer.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Sends the reminder email via Resend's HTTP API (https://resend.com) —\n// chosen over SMTP specifically so this stays a plain `fetch` call with\n// zero npm dependencies, same as every other MultiNicheAI automation (see\n// supplier-po-sync's lib/mailer.mjs for the exact pattern this mirrors).\n\nconst API_URL = 'https://api.resend.com/emails'\n\n/**\n * @param {object} email\n * @param {string} email.to\n * @param {string} email.subject\n * @param {string} email.text\n * @param {string} [email.html]\n * @param {string} [email.fromName]\n * @param {string} [email.fromEmail]\n * @param {string} [apiKey]  defaults to RESEND_API_KEY\n * @returns {Promise<boolean>} true if sent, false if no key configured (a\n *   deliberate no-op, same pattern as the Slack webhook helper, so a dry\n *   run/test doesn't need a real account)\n */\nexport async function sendReminderEmail(email, apiKey = process.env.RESEND_API_KEY) {\n  if (!apiKey) {\n    console.warn('RESEND_API_KEY is not set — skipping send (dry run).')\n    return false\n  }\n  if (!email.to) throw new Error('sendReminderEmail requires email.to (the customer\\'s address).')\n\n  const fromName = email.fromName ?? 'Billing'\n  const fromEmail = email.fromEmail ?? process.env.CHASER_FROM_EMAIL\n  if (!fromEmail) throw new Error('Set email.fromEmail or CHASER_FROM_EMAIL — Resend requires a verified sending domain.')\n\n  const body = {\n    from: `${fromName} <${fromEmail}>`,\n    to: [email.to],\n    subject: email.subject,\n    text: email.text,\n  }\n  if (email.html) body.html = email.html\n\n  const res = await fetch(API_URL, {\n    method: 'POST',\n    headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },\n    body: JSON.stringify(body),\n    signal: AbortSignal.timeout(15_000),\n  })\n  if (!res.ok) {\n    const detail = await res.text().catch(() => '')\n    throw new Error(`Resend API returned HTTP ${res.status}: ${detail.slice(0, 300)}`)\n  }\n  return true\n}\n",
  },
  {
    path: "lib/overdue.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// The two decisions this whole product makes: how overdue is this invoice,\n// and — given the escalation ladder and what's already been sent for it —\n// which threshold (if any) needs a reminder right now. Kept as their own\n// tiny, pure module so both have a test independent of the Stripe/email\n// plumbing around them.\n\n/**\n * @param {string|Date} dueDate  an ISO date/datetime string, or a Date\n * @param {Date} [now]\n * @returns {number} whole days past due; 0 or negative means not overdue\n */\nexport function daysOverdue(dueDate, now = new Date()) {\n  const due = dueDate instanceof Date ? dueDate : new Date(dueDate)\n  if (Number.isNaN(due.getTime())) throw new Error(`Invalid due date: ${dueDate}`)\n  const diffMs = now.getTime() - due.getTime()\n  return Math.floor(diffMs / 86_400_000)\n}\n\n/**\n * Picks the escalation threshold to send a reminder at, given how many days\n * an invoice is overdue and which threshold-days have already had a\n * reminder sent for this invoice. Always returns the HIGHEST crossed\n * threshold that hasn't been sent yet — so an invoice that goes unchecked\n * for a while (crossing two thresholds between runs) gets one reminder at\n * the more urgent tone, not a flood of catch-up emails.\n *\n * @param {number} daysPastDue\n * @param {Array<{days:number,label:string,tone?:string}>} thresholds\n * @param {number[]} [alreadySentDays]\n * @returns {{days:number,label:string,tone?:string}|null}\n */\nexport function selectThresholdToSend(daysPastDue, thresholds, alreadySentDays = []) {\n  if (!Number.isFinite(daysPastDue) || daysPastDue < 0) return null\n  const sent = new Set(alreadySentDays)\n  let best = null\n  for (const threshold of thresholds) {\n    if (threshold.days > daysPastDue) continue\n    if (sent.has(threshold.days)) continue\n    if (!best || threshold.days > best.days) best = threshold\n  }\n  return best\n}\n",
  },
  {
    path: "lib/reminder-email.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Turns an overdue invoice + the escalation threshold it crossed into an\n// actual reminder email (subject, HTML body, plain-text body). No\n// templating engine — a reminder is a handful of facts and a tone, and\n// keeping this dependency-free means it has nothing to break on an update.\n\nfunction formatMoney(amountInCents, currency = 'usd') {\n  const amount = (Number(amountInCents) / 100).toFixed(2)\n  return `${currency.toUpperCase()} ${amount}`\n}\n\nfunction escapeHtml(value) {\n  return String(value ?? '')\n    .replace(/&/g, '&amp;')\n    .replace(/</g, '&lt;')\n    .replace(/>/g, '&gt;')\n    .replace(/\"/g, '&quot;')\n}\n\nconst TONE_OPENERS = {\n  friendly: \"Just a friendly heads up — it looks like the invoice below hasn't been paid yet.\",\n  firm: 'This invoice is now significantly overdue and needs your attention.',\n  final: 'This is a final notice: the invoice below remains unpaid well past its due date.',\n  neutral: 'The invoice below is now overdue.',\n}\n\n/**\n * @param {object} args\n * @param {object} args.invoice  {id, number, customerName, customerEmail,\n *   amountDue, currency, dueDate, hostedInvoiceUrl, daysOverdue}\n * @param {{days:number,label:string,tone?:string}} args.threshold\n * @param {string} [args.storeName]\n * @returns {{subject:string, text:string, html:string}}\n */\nexport function buildReminderEmail({ invoice, threshold, storeName }) {\n  if (!invoice) throw new Error('buildReminderEmail requires invoice.')\n  if (!threshold) throw new Error('buildReminderEmail requires threshold.')\n\n  const invoiceLabel = invoice.number ?? invoice.id\n  const amount = formatMoney(invoice.amountDue, invoice.currency)\n  const dayWord = invoice.daysOverdue === 1 ? 'day' : 'days'\n  const subjectPrefix = storeName ? `${storeName}: ` : ''\n  const subject = `${subjectPrefix}${threshold.label} — invoice ${invoiceLabel} is ${invoice.daysOverdue} ${dayWord} overdue`\n\n  const greeting = invoice.customerName ? `Hi ${invoice.customerName},` : 'Hi,'\n  const opener = TONE_OPENERS[threshold.tone] ?? TONE_OPENERS.neutral\n  const dueDate = new Date(invoice.dueDate).toISOString().slice(0, 10)\n\n  const textLines = [\n    greeting,\n    '',\n    opener,\n    '',\n    `  Invoice: ${invoiceLabel}`,\n    `  Amount due: ${amount}`,\n    `  Due date: ${dueDate}`,\n    `  Days overdue: ${invoice.daysOverdue}`,\n  ]\n  if (invoice.hostedInvoiceUrl) {\n    textLines.push(`  Pay now: ${invoice.hostedInvoiceUrl}`)\n  }\n  textLines.push('', 'If you\\'ve already paid, please disregard this reminder — it may just be crossing with your payment.')\n  if (storeName) textLines.push('', storeName)\n  const text = textLines.join('\\n')\n\n  const html = [\n    `<p>${escapeHtml(greeting)}</p>`,\n    `<p>${escapeHtml(opener)}</p>`,\n    '<table cellpadding=\"4\" cellspacing=\"0\">',\n    `<tr><td><strong>Invoice</strong></td><td>${escapeHtml(invoiceLabel)}</td></tr>`,\n    `<tr><td><strong>Amount due</strong></td><td>${escapeHtml(amount)}</td></tr>`,\n    `<tr><td><strong>Due date</strong></td><td>${escapeHtml(dueDate)}</td></tr>`,\n    `<tr><td><strong>Days overdue</strong></td><td>${escapeHtml(String(invoice.daysOverdue))}</td></tr>`,\n    '</table>',\n    invoice.hostedInvoiceUrl\n      ? `<p><a href=\"${escapeHtml(invoice.hostedInvoiceUrl)}\">Pay this invoice</a></p>`\n      : '',\n    \"<p>If you've already paid, please disregard this reminder — it may just be crossing with your payment.</p>\",\n    storeName ? `<p>${escapeHtml(storeName)}</p>` : '',\n  ].filter(Boolean).join('\\n')\n\n  return { subject, text, html }\n}\n",
  },
  {
    path: "lib/slack.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Optional run digest, so you don't have to check your inbox/logs to know\n// the job ran: how many invoices are overdue, how many reminders actually\n// went out, and how much money is outstanding. A no-op if\n// SLACK_WEBHOOK_URL isn't set.\n\n/**\n * @param {object} summary\n * @param {number} summary.overdueCount\n * @param {Array<{invoiceLabel:string, thresholdLabel:string, sent:boolean}>} summary.reminders\n * @param {number} summary.totalOutstandingCents\n * @param {string} [summary.currency]\n * @param {string|undefined} webhookUrl\n * @returns {Promise<boolean>}\n */\nexport async function postSummary(summary, webhookUrl) {\n  if (!webhookUrl) return false\n\n  const currency = (summary.currency ?? 'usd').toUpperCase()\n  const totalOutstanding = (summary.totalOutstandingCents / 100).toFixed(2)\n\n  const lines = [\n    `Invoice chaser ran — ${summary.overdueCount} overdue invoice(s), ${currency} ${totalOutstanding} outstanding.`,\n  ]\n  if (summary.reminders.length) {\n    lines.push(`Reminders (${summary.reminders.length}):`)\n    for (const r of summary.reminders) {\n      lines.push(`• ${r.invoiceLabel} — ${r.thresholdLabel}${r.sent ? '' : ' — NOT sent (dry run / no email key)'}`)\n    }\n  } else {\n    lines.push('No new thresholds crossed — nothing to remind.')\n  }\n\n  try {\n    const res = await fetch(webhookUrl, {\n      method: 'POST',\n      headers: { 'content-type': 'application/json' },\n      body: JSON.stringify({ text: lines.join('\\n') }),\n      signal: AbortSignal.timeout(8000),\n    })\n    if (!res.ok) {\n      console.warn(`Slack webhook returned HTTP ${res.status}`)\n      return false\n    }\n    return true\n  } catch (error) {\n    console.warn(`Slack webhook failed: ${error?.message ?? error}`)\n    return false\n  }\n}\n",
  },
  {
    path: "lib/state.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Dedupe state: which escalation thresholds have already fired a reminder\n// for which invoice, so re-running the job (daily, or twice in one day\n// after a fix) never re-sends the same threshold. Deliberately a single\n// flat JSON file, not a database — same pattern every zero-dependency\n// product in this repo uses for state that needs to survive between runs.\n// The read/write helpers touch the filesystem; the lookup/update helpers\n// are plain object functions so the dedupe logic itself has a test that\n// doesn't need a real file on disk.\n\nimport { readFileSync, writeFileSync, existsSync } from 'node:fs'\n\n/**\n * @param {string} path\n * @returns {Record<string, {sentThresholds:number[], lastSentAt?:string}>}\n */\nexport function loadState(path) {\n  if (!existsSync(path)) return {}\n  let raw\n  try {\n    raw = readFileSync(path, 'utf8')\n  } catch (err) {\n    throw new Error(`Could not read state file at \"${path}\": ${err.message}`)\n  }\n  if (!raw.trim()) return {}\n  try {\n    return JSON.parse(raw)\n  } catch (err) {\n    throw new Error(`\"${path}\" is not valid JSON: ${err.message}`)\n  }\n}\n\n/**\n * @param {string} path\n * @param {object} state\n */\nexport function saveState(path, state) {\n  writeFileSync(path, JSON.stringify(state, null, 2) + '\\n')\n}\n\n/**\n * @param {object} state\n * @param {string} invoiceId\n * @returns {number[]}\n */\nexport function getSentThresholds(state, invoiceId) {\n  return state[invoiceId]?.sentThresholds ?? []\n}\n\n/**\n * @param {object} state\n * @param {string} invoiceId\n * @param {number} days\n * @returns {boolean}\n */\nexport function hasReminderBeenSent(state, invoiceId, days) {\n  return getSentThresholds(state, invoiceId).includes(days)\n}\n\n/**\n * Records that a reminder for `days` went out for `invoiceId`. Mutates and\n * returns `state` so callers can chain it across a batch of invoices before\n * a single saveState() call at the end of the run.\n *\n * @param {object} state\n * @param {string} invoiceId\n * @param {number} days\n * @param {string} [sentAt]  ISO timestamp; defaults to now\n * @returns {object} state\n */\nexport function recordReminderSent(state, invoiceId, days, sentAt = new Date().toISOString()) {\n  const entry = state[invoiceId] ?? { sentThresholds: [] }\n  if (!entry.sentThresholds.includes(days)) entry.sentThresholds.push(days)\n  entry.lastSentAt = sentAt\n  state[invoiceId] = entry\n  return state\n}\n\n/**\n * Drops entries for invoices no longer present in the current Stripe pull\n * (i.e. paid, voided, or deleted) so state.json doesn't grow forever.\n *\n * @param {object} state\n * @param {string[]} currentInvoiceIds\n * @returns {object} a new state object containing only current invoices\n */\nexport function pruneState(state, currentInvoiceIds) {\n  const keep = new Set(currentInvoiceIds)\n  const next = {}\n  for (const [id, entry] of Object.entries(state)) {\n    if (keep.has(id)) next[id] = entry\n  }\n  return next\n}\n",
  },
  {
    path: "lib/stripe-client.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Reads real open invoices from Stripe's REST API directly — no stripe-node\n// dependency, same zero-dependency pattern as every other MultiNicheAI\n// automation (see how supplier-po-sync's lib/shopify-client.mjs calls\n// Shopify with plain fetch). A restricted API key with read access to\n// Invoices is all this needs; it never creates, updates, or voids anything\n// in Stripe.\n\nconst API_BASE = 'https://api.stripe.com/v1'\n\nfunction authHeaders(secretKey) {\n  const key = secretKey ?? process.env.STRIPE_SECRET_KEY\n  if (!key) throw new Error('STRIPE_SECRET_KEY is not set.')\n  // Stripe uses HTTP Basic auth with the secret key as the username and an\n  // empty password — not a Bearer token.\n  return { Authorization: `Basic ${Buffer.from(`${key}:`).toString('base64')}` }\n}\n\n/**\n * Normalizes a raw Stripe invoice object into the flat shape the rest of\n * this product works with.\n *\n * @param {object} inv  raw Stripe invoice object\n * @returns {{id:string, number:string|null, customerName:string|null,\n *   customerEmail:string|null, amountDue:number, currency:string,\n *   dueDate:string, hostedInvoiceUrl:string|null}}\n */\nexport function normalizeInvoice(inv) {\n  return {\n    id: inv.id,\n    number: inv.number ?? null,\n    customerName: inv.customer_name ?? null,\n    customerEmail: inv.customer_email ?? null,\n    amountDue: Number(inv.amount_due ?? 0),\n    currency: inv.currency ?? 'usd',\n    dueDate: new Date(inv.due_date * 1000).toISOString(),\n    hostedInvoiceUrl: inv.hosted_invoice_url ?? null,\n  }\n}\n\n/**\n * Fetches every open Stripe invoice that has a due date set (an invoice\n * with no due date, e.g. one paid on receipt with no terms, has nothing\n * for this product to chase), paginating through Stripe's cursor API.\n *\n * @param {object} [options]\n * @param {string} [options.secretKey]  defaults to STRIPE_SECRET_KEY\n * @returns {Promise<Array<ReturnType<typeof normalizeInvoice>>>}\n */\nexport async function getOpenInvoices(options = {}) {\n  const headers = authHeaders(options.secretKey)\n  const invoices = []\n  let startingAfter\n\n  do {\n    const params = new URLSearchParams({ status: 'open', limit: '100' })\n    if (startingAfter) params.set('starting_after', startingAfter)\n\n    const res = await fetch(`${API_BASE}/invoices?${params.toString()}`, {\n      headers,\n      signal: AbortSignal.timeout(15_000),\n    })\n    if (!res.ok) {\n      const detail = await res.text().catch(() => '')\n      throw new Error(`Stripe API returned HTTP ${res.status}: ${detail.slice(0, 300)}`)\n    }\n    const data = await res.json()\n    const page = data.data ?? []\n\n    for (const inv of page) {\n      if (!inv.due_date) continue // nothing to chase without a due date\n      invoices.push(normalizeInvoice(inv))\n    }\n\n    startingAfter = data.has_more && page.length ? page[page.length - 1].id : null\n  } while (startingAfter)\n\n  return invoices\n}\n",
  },
  {
    path: "bin/chase.mjs",
    contents: "#!/usr/bin/env node\n// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Entry point: reads config.json (the escalation ladder), pulls every real\n// open Stripe invoice with a due date, works out which ones are overdue and\n// which escalation threshold (if any) each one has newly crossed, sends a\n// reminder email per invoice that needs one, and records what was sent in\n// state.json so the same threshold never re-fires. Safe to run as often as\n// you like — an invoice that hasn't crossed a new threshold isn't touched.\n\nimport { loadConfig } from '../lib/config.mjs'\nimport { getOpenInvoices } from '../lib/stripe-client.mjs'\nimport { daysOverdue, selectThresholdToSend } from '../lib/overdue.mjs'\nimport { buildReminderEmail } from '../lib/reminder-email.mjs'\nimport { sendReminderEmail } from '../lib/mailer.mjs'\nimport { postSummary } from '../lib/slack.mjs'\nimport { loadState, saveState, getSentThresholds, recordReminderSent, pruneState } from '../lib/state.mjs'\n\nasync function main() {\n  const configPath = process.env.CHASER_CONFIG_PATH ?? new URL('../config.json', import.meta.url).pathname\n  const statePath = process.env.CHASER_STATE_PATH ?? new URL('../state.json', import.meta.url).pathname\n\n  const thresholds = loadConfig(configPath)\n  let state = loadState(statePath)\n\n  const invoices = await getOpenInvoices()\n  const now = new Date()\n\n  const reminders = []\n  let totalOutstandingCents = 0\n  let currency = 'usd'\n\n  for (const invoice of invoices) {\n    totalOutstandingCents += invoice.amountDue\n    currency = invoice.currency ?? currency\n\n    const overdueBy = daysOverdue(invoice.dueDate, now)\n    if (overdueBy <= 0) continue // not overdue yet\n\n    const alreadySent = getSentThresholds(state, invoice.id)\n    const threshold = selectThresholdToSend(overdueBy, thresholds, alreadySent)\n    if (!threshold) continue // nothing new crossed for this invoice\n\n    const withDays = { ...invoice, daysOverdue: overdueBy }\n    const email = buildReminderEmail({ invoice: withDays, threshold, storeName: process.env.STORE_NAME })\n\n    let sent = false\n    if (invoice.customerEmail) {\n      sent = await sendReminderEmail({\n        to: invoice.customerEmail,\n        subject: email.subject,\n        text: email.text,\n        html: email.html,\n        fromName: process.env.STORE_NAME,\n      })\n    } else {\n      console.warn(`Invoice ${invoice.number ?? invoice.id} has no customer email — skipping send.`)\n    }\n\n    if (sent) {\n      state = recordReminderSent(state, invoice.id, threshold.days, now.toISOString())\n    }\n\n    const invoiceLabel = invoice.number ?? invoice.id\n    console.log(`${sent ? 'Sent' : 'Built (not sent)'} \"${threshold.label}\" reminder for ${invoiceLabel} (${overdueBy} days overdue)`)\n    reminders.push({ invoiceLabel, thresholdLabel: threshold.label, sent })\n  }\n\n  if (reminders.length === 0) {\n    console.log('No invoice crossed a new escalation threshold this run.')\n  }\n\n  state = pruneState(state, invoices.map((i) => i.id))\n  saveState(statePath, state)\n\n  await postSummary(\n    {\n      overdueCount: invoices.filter((i) => daysOverdue(i.dueDate, now) > 0).length,\n      reminders,\n      totalOutstandingCents,\n      currency,\n    },\n    process.env.SLACK_WEBHOOK_URL,\n  )\n}\n\nmain().catch((err) => {\n  console.error(`invoice-chaser failed: ${err.message}`)\n  process.exitCode = 1\n})\n",
  },
  {
    path: ".github-workflow-template/invoice-chaser.yml",
    contents: "# Copy this file to .github/workflows/invoice-chaser.yml in the repo you run\n# this from (this exact path — GitHub only runs workflows from\n# .github/workflows/). Also copy bin/, lib/, and your own config.json\n# somewhere in that repo (e.g. tools/invoice-chaser/) and adjust the\n# `working-directory` below to match.\n#\n# Repo secrets required (Settings -> Secrets and variables -> Actions):\n#   STRIPE_SECRET_KEY     from dashboard.stripe.com/apikeys — restricted key\n#                          with read access to Invoices is enough\n#   RESEND_API_KEY        from resend.com — omit to dry-run (computes\n#                          reminders, logs them, sends nothing, and does not\n#                          mark thresholds as sent)\n#   CHASER_FROM_EMAIL     a verified sending address on your Resend domain\n# Optional:\n#   SLACK_WEBHOOK_URL     posts a one-line run digest\n#   STORE_NAME            used in the reminder subject/body\n#\n# state.json (which thresholds have already fired per invoice) has to\n# persist between runs or every run will look like the first one. This\n# workflow commits it back to the repo after each run — simplest option for\n# a single-branch, single-runner schedule. If you'd rather not commit a\n# generated file, swap the \"Persist state.json\" step for actions/cache or\n# your own artifact storage.\n\nname: Invoice chaser\n\non:\n  schedule:\n    - cron: '0 13 * * *' # once daily, 1pm UTC — adjust to your business hours\n  workflow_dispatch: {} # lets you trigger it manually from the Actions tab too\n\njobs:\n  chase:\n    runs-on: ubuntu-latest\n    permissions:\n      contents: write # needed to commit the updated state.json back\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n\n      - name: Run invoice chaser\n        working-directory: tools/invoice-chaser # <- adjust to wherever you put lib/, bin/, config.json\n        env:\n          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}\n          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}\n          CHASER_FROM_EMAIL: ${{ secrets.CHASER_FROM_EMAIL }}\n          STORE_NAME: ${{ secrets.STORE_NAME }}\n          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}\n        run: node bin/chase.mjs\n\n      - name: Persist state.json\n        working-directory: tools/invoice-chaser\n        run: |\n          if [ -n \"$(git status --porcelain state.json)\" ]; then\n            git config user.name \"invoice-chaser bot\"\n            git config user.email \"actions@users.noreply.github.com\"\n            git add state.json\n            git commit -m \"invoice-chaser: update reminder state [skip ci]\"\n            git push\n          fi\n",
  },
  {
    path: "test/run.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nimport assert from 'node:assert/strict'\nimport { writeFileSync, unlinkSync } from 'node:fs'\nimport { daysOverdue, selectThresholdToSend } from '../lib/overdue.mjs'\nimport {\n  loadState,\n  saveState,\n  hasReminderBeenSent,\n  recordReminderSent,\n  getSentThresholds,\n  pruneState,\n} from '../lib/state.mjs'\nimport { buildReminderEmail } from '../lib/reminder-email.mjs'\nimport { sendReminderEmail } from '../lib/mailer.mjs'\nimport { loadConfig } from '../lib/config.mjs'\nimport { normalizeInvoice } from '../lib/stripe-client.mjs'\n\nlet failures = 0\nasync function test(name, fn) {\n  try {\n    await fn()\n    console.log(`ok - ${name}`)\n  } catch (err) {\n    failures++\n    console.error(`not ok - ${name}`)\n    console.error(`  ${err.message}`)\n  }\n}\n\nconst THRESHOLDS = [\n  { days: 3, label: 'Friendly reminder', tone: 'friendly' },\n  { days: 7, label: 'Firmer reminder', tone: 'firm' },\n  { days: 14, label: 'Final notice', tone: 'final' },\n]\n\n// ---- daysOverdue -----------------------------------------------------------\n\nawait test('daysOverdue: counts whole days past a due date', () => {\n  const due = new Date('2026-01-01T00:00:00Z')\n  const now = new Date('2026-01-06T00:00:00Z')\n  assert.equal(daysOverdue(due, now), 5)\n})\n\nawait test('daysOverdue: zero on the due date itself, negative before it', () => {\n  const due = new Date('2026-01-01T00:00:00Z')\n  assert.equal(daysOverdue(due, new Date('2026-01-01T00:00:00Z')), 0)\n  assert.equal(daysOverdue(due, new Date('2025-12-30T00:00:00Z')), -2)\n})\n\nawait test('daysOverdue: accepts an ISO string, not just a Date', () => {\n  assert.equal(daysOverdue('2026-01-01T00:00:00Z', new Date('2026-01-04T00:00:00Z')), 3)\n})\n\nawait test('daysOverdue: rejects an invalid date', () => {\n  assert.throws(() => daysOverdue('not-a-date'), /Invalid due date/)\n})\n\n// ---- selectThresholdToSend --------------------------------------------------\n\nawait test('selectThresholdToSend: not overdue yet -> nothing to send', () => {\n  assert.equal(selectThresholdToSend(-1, THRESHOLDS, []), null)\n  assert.equal(selectThresholdToSend(0, THRESHOLDS, []), null)\n})\n\nawait test('selectThresholdToSend: below the first threshold -> nothing to send', () => {\n  assert.equal(selectThresholdToSend(2, THRESHOLDS, []), null)\n})\n\nawait test('selectThresholdToSend: crosses first threshold, none sent yet', () => {\n  const t = selectThresholdToSend(5, THRESHOLDS, [])\n  assert.equal(t.days, 3)\n})\n\nawait test('selectThresholdToSend: already sent the only crossed threshold -> nothing new', () => {\n  assert.equal(selectThresholdToSend(5, THRESHOLDS, [3]), null)\n})\n\nawait test('selectThresholdToSend: catches up to the highest crossed-but-unsent threshold, not a flood', () => {\n  // Gone unchecked long enough to cross both day-3 and day-7; day-3 already\n  // sent. Should escalate straight to day-7, not re-send day-3.\n  const t = selectThresholdToSend(10, THRESHOLDS, [3])\n  assert.equal(t.days, 7)\n})\n\nawait test('selectThresholdToSend: all thresholds already sent -> nothing to send', () => {\n  assert.equal(selectThresholdToSend(30, THRESHOLDS, [3, 7, 14]), null)\n})\n\nawait test('selectThresholdToSend: exactly at a threshold day counts as crossed', () => {\n  const t = selectThresholdToSend(7, THRESHOLDS, [])\n  assert.equal(t.days, 7)\n})\n\n// ---- state (dedupe tracking) ------------------------------------------------\n\nawait test('state: hasReminderBeenSent/recordReminderSent round-trip', () => {\n  let state = {}\n  assert.equal(hasReminderBeenSent(state, 'in_123', 3), false)\n  state = recordReminderSent(state, 'in_123', 3, '2026-01-05T00:00:00Z')\n  assert.equal(hasReminderBeenSent(state, 'in_123', 3), true)\n  assert.equal(hasReminderBeenSent(state, 'in_123', 7), false)\n  assert.deepEqual(getSentThresholds(state, 'in_123'), [3])\n})\n\nawait test('state: recording the same threshold twice does not duplicate it', () => {\n  let state = {}\n  state = recordReminderSent(state, 'in_1', 3)\n  state = recordReminderSent(state, 'in_1', 3)\n  assert.deepEqual(getSentThresholds(state, 'in_1'), [3])\n})\n\nawait test('state: tracks multiple invoices independently', () => {\n  let state = {}\n  state = recordReminderSent(state, 'in_a', 3)\n  state = recordReminderSent(state, 'in_b', 7)\n  assert.deepEqual(getSentThresholds(state, 'in_a'), [3])\n  assert.deepEqual(getSentThresholds(state, 'in_b'), [7])\n})\n\nawait test('state: pruneState drops invoices no longer present', () => {\n  let state = {}\n  state = recordReminderSent(state, 'in_stale', 3)\n  state = recordReminderSent(state, 'in_current', 3)\n  const pruned = pruneState(state, ['in_current'])\n  assert.deepEqual(Object.keys(pruned), ['in_current'])\n})\n\nawait test('state: loadState returns {} for a missing file', () => {\n  const path = new URL('./tmp-missing-state.json', import.meta.url).pathname\n  assert.deepEqual(loadState(path), {})\n})\n\nawait test('state: saveState/loadState round-trip through disk', () => {\n  const path = new URL('./tmp-state.json', import.meta.url).pathname\n  let state = recordReminderSent({}, 'in_9', 14, '2026-02-01T00:00:00Z')\n  saveState(path, state)\n  try {\n    const reloaded = loadState(path)\n    assert.equal(hasReminderBeenSent(reloaded, 'in_9', 14), true)\n  } finally {\n    unlinkSync(path)\n  }\n})\n\n// ---- buildReminderEmail ------------------------------------------------------\n\nawait test('buildReminderEmail: names the customer, amount, and days overdue', () => {\n  const invoice = {\n    id: 'in_1',\n    number: 'INV-001',\n    customerName: 'Dana Ortiz',\n    customerEmail: 'dana@example.com',\n    amountDue: 45000,\n    currency: 'usd',\n    dueDate: '2026-01-01T00:00:00Z',\n    hostedInvoiceUrl: 'https://pay.stripe.com/invoice/xyz',\n    daysOverdue: 5,\n  }\n  const email = buildReminderEmail({ invoice, threshold: THRESHOLDS[0], storeName: 'Acme Studio' })\n  assert.match(email.subject, /INV-001/)\n  assert.match(email.subject, /5 days overdue/)\n  assert.match(email.text, /Dana Ortiz/)\n  assert.match(email.text, /USD 450\\.00/)\n  assert.match(email.text, /pay\\.stripe\\.com/)\n  assert.match(email.html, /Dana Ortiz/)\n  assert.match(email.html, /pay\\.stripe\\.com/)\n})\n\nawait test('buildReminderEmail: falls back to a generic greeting with no customer name', () => {\n  const invoice = {\n    id: 'in_2', number: null, customerName: null, customerEmail: 'x@example.com',\n    amountDue: 1000, currency: 'usd', dueDate: '2026-01-01T00:00:00Z',\n    hostedInvoiceUrl: null, daysOverdue: 1,\n  }\n  const email = buildReminderEmail({ invoice, threshold: THRESHOLDS[0] })\n  assert.match(email.text, /^Hi,/)\n  assert.match(email.subject, /1 day overdue/) // singular, not \"1 days\"\n})\n\nawait test('buildReminderEmail: escapes HTML in the customer name', () => {\n  const invoice = {\n    id: 'in_3', number: 'INV-3', customerName: '<script>alert(1)</script>', customerEmail: 'x@example.com',\n    amountDue: 500, currency: 'usd', dueDate: '2026-01-01T00:00:00Z',\n    hostedInvoiceUrl: null, daysOverdue: 4,\n  }\n  const email = buildReminderEmail({ invoice, threshold: THRESHOLDS[0] })\n  assert.ok(!email.html.includes('<script>'))\n  assert.match(email.html, /&lt;script&gt;/)\n})\n\nawait test('buildReminderEmail: requires invoice and threshold', () => {\n  assert.throws(() => buildReminderEmail({ threshold: THRESHOLDS[0] }), /invoice/)\n  assert.throws(() => buildReminderEmail({ invoice: {} }), /threshold/)\n})\n\n// ---- mailer -------------------------------------------------------------------\n\nawait test('sendReminderEmail: is a no-op (dry run) with no API key configured', async () => {\n  const sent = await sendReminderEmail({ to: 'a@b.example', subject: 'x', text: 'x' }, '')\n  assert.equal(sent, false)\n})\n\nawait test('sendReminderEmail: rejects a missing recipient even with a key present', async () => {\n  await assert.rejects(() => sendReminderEmail({ subject: 'x', text: 'x' }, 'fake-key'), /email\\.to/)\n})\n\n// ---- config -------------------------------------------------------------------\n\nawait test('loadConfig: rejects a config entry missing a required field', () => {\n  const path = new URL('./tmp-bad-config.json', import.meta.url).pathname\n  writeFileSync(path, JSON.stringify([{ days: 3 }]))\n  try {\n    assert.throws(() => loadConfig(path), /missing required field/)\n  } finally {\n    unlinkSync(path)\n  }\n})\n\nawait test('loadConfig: rejects a non-positive threshold', () => {\n  const path = new URL('./tmp-bad-days.json', import.meta.url).pathname\n  writeFileSync(path, JSON.stringify([{ days: 0, label: 'x' }]))\n  try {\n    assert.throws(() => loadConfig(path), /positive integer/)\n  } finally {\n    unlinkSync(path)\n  }\n})\n\nawait test('loadConfig: rejects a duplicate threshold day', () => {\n  const path = new URL('./tmp-dup-days.json', import.meta.url).pathname\n  writeFileSync(path, JSON.stringify([{ days: 3, label: 'a' }, { days: 3, label: 'b' }]))\n  try {\n    assert.throws(() => loadConfig(path), /more than one threshold/)\n  } finally {\n    unlinkSync(path)\n  }\n})\n\nawait test('loadConfig: sorts thresholds ascending by day regardless of input order', () => {\n  const path = new URL('./tmp-unsorted.json', import.meta.url).pathname\n  writeFileSync(path, JSON.stringify([{ days: 14, label: 'c' }, { days: 3, label: 'a' }, { days: 7, label: 'b' }]))\n  try {\n    const config = loadConfig(path)\n    assert.deepEqual(config.map((c) => c.days), [3, 7, 14])\n  } finally {\n    unlinkSync(path)\n  }\n})\n\nawait test('loadConfig: accepts the shipped config.example.json as-is', () => {\n  const path = new URL('../config.example.json', import.meta.url).pathname\n  const config = loadConfig(path)\n  assert.ok(config.length >= 1)\n})\n\n// ---- stripe-client normalization ------------------------------------------\n\nawait test('normalizeInvoice: maps Stripe fields into the flat internal shape', () => {\n  const raw = {\n    id: 'in_abc',\n    number: 'INV-42',\n    customer_name: 'Jordan Lee',\n    customer_email: 'jordan@example.com',\n    amount_due: 12345,\n    currency: 'usd',\n    due_date: Math.floor(new Date('2026-03-01T00:00:00Z').getTime() / 1000),\n    hosted_invoice_url: 'https://pay.stripe.com/invoice/abc',\n  }\n  const normalized = normalizeInvoice(raw)\n  assert.equal(normalized.id, 'in_abc')\n  assert.equal(normalized.number, 'INV-42')\n  assert.equal(normalized.amountDue, 12345)\n  assert.equal(normalized.dueDate, '2026-03-01T00:00:00.000Z')\n  assert.equal(normalized.hostedInvoiceUrl, 'https://pay.stripe.com/invoice/abc')\n})\n\nawait test('normalizeInvoice: tolerates missing optional fields', () => {\n  const raw = { id: 'in_min', amount_due: 0, due_date: 1700000000 }\n  const normalized = normalizeInvoice(raw)\n  assert.equal(normalized.number, null)\n  assert.equal(normalized.customerName, null)\n  assert.equal(normalized.currency, 'usd')\n})\n\nconsole.log(failures === 0 ? '\\nAll checks passed.' : `\\n${failures} check(s) failed.`)\nprocess.exitCode = failures === 0 ? 0 : 1\n",
  },
]
