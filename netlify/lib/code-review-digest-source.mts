// GENERATED FILE — DO NOT EDIT BY HAND.
//
// Produced by packages/code-review-digest/tools/embed-source.mjs from the real
// package source. Regenerate after changing the package:
//
//   node packages/code-review-digest/tools/embed-source.mjs
//
// This is the payload for the Code Review Digest product (SKU AI-AB-013): the
// complete, runnable source the buyer receives at checkout. It is embedded
// rather than read from disk so fulfilment cannot fail on a missing file.

export interface SourceFile {
  path: string
  contents: string
}

export const CODE_REVIEW_DIGEST_SOURCE: SourceFile[] = [
  {
    path: "README.md",
    contents: "# Code Review Digest\n\nA GitHub Actions workflow that posts a real, prioritized PR digest to Slack\non a schedule. Not a blueprint you wire yourself in an automation tool — an\nactual `.yml` workflow that runs itself once it's in your repo.\n\n## What it does, precisely\n\n1. On a schedule (default: 14:00 UTC weekdays — edit the cron in the\n   workflow), it lists every open PR via GitHub's API.\n2. For each one it pulls the latest review state, age, and last-updated time.\n3. Claude turns that into a digest that actually prioritizes — \"these 3 need\n   a human, these 2 are fine to leave\" — not a flat list in PR-number order.\n4. Posts it to Slack as a real Block Kit message.\n\nIt does not review code itself, approve/block PRs, or replace a human\nreviewer. It tells you where to look.\n\n## Install (5 minutes)\n\n1. Copy `bin/` and `lib/` into your repo — anywhere, e.g.\n   `tools/code-review-digest/`.\n2. Copy `.github-workflow-template/code-review-digest.yml` to\n   `.github/workflows/code-review-digest.yml` (this exact path — GitHub only\n   runs workflows from there).\n3. If you put `bin/`/`lib/` somewhere other than `tools/code-review-digest/`,\n   edit the `working-directory:` line in the workflow to match.\n4. Add two repo secrets (**Settings → Secrets and variables → Actions**):\n   - `ANTHROPIC_API_KEY` — your own key from console.anthropic.com\n   - `SLACK_WEBHOOK_URL` — Slack → Apps → Incoming Webhooks\n5. Commit and push. It runs on the schedule automatically, or trigger it once\n   immediately from the **Actions** tab (`workflow_dispatch`) to check it\n   actually works before waiting for the cron.\n\n`GITHUB_TOKEN` needs no secret of its own — Actions provides it, scoped\nread-only by the `permissions:` block already in the workflow.\n\n## Testing without waiting for Actions\n\n```sh\nnpm install --omit=dev\ncp .env.example .env    # fill in ANTHROPIC_API_KEY, SLACK_WEBHOOK_URL\nGITHUB_TOKEN=ghp_your_own_token GITHUB_REPOSITORY=you/your-repo node bin/digest.mjs\n```\n\n`GITHUB_TOKEN` here can be a personal access token with `repo` read scope —\njust for local testing; inside Actions itself you never create or store one.\n\n```sh\nnpm test\n```\nRuns without real API keys — checks the Slack formatter and error handling,\nnot a live GitHub/Claude call.\n",
  },
  {
    path: "LICENSE.md",
    contents: "# License\n\n**code-review-digest — perpetual single-purchase license**\n\n> This is a plain-language commercial license template. It has not been reviewed\n> by a lawyer. Have one look at it before you sell against it, and replace\n> `[SELLER]` and `[JURISDICTION]` with your details.\n\n## The short version\n\nYou bought it once. You own your copy forever. Run it against as many of **your own** repositories as you like. Do not\nresell it as a product of its own.\n\n## What you may do\n\n- Use the software for any purpose, commercial or personal, forever.\n- Run it on unlimited repositories that you own or operate.\n- Use it in client work, and use the digests it produces however you like.\n- Modify the source freely. It is plain JavaScript with no build step precisely so\n  that you can.\n- Keep using it indefinitely. There is no license key, no activation, no expiry,\n  no phone-home, and nothing that stops working if [SELLER] does.\n- Keep and use any version you have received, forever, regardless of what happens\n  to later versions or to [SELLER].\n\n## What you may not do\n\n- Resell, relicense, sublicense or redistribute the software itself, in whole or\n  in substantial part, as a product or as part of a product whose value is\n  substantially this software.\n- Publish the source publicly, or include it in a public repository, package\n  registry, or template that others can obtain without buying it.\n- Remove or alter this license file or the attribution in the source headers.\n\nSelling the *output* — review reports, engineering-process consulting — is\nexplicitly allowed. Selling *the tool* is not.\n\n## Updates\n\nAny updates published within twelve months of your purchase are included at no\nextra cost. After that, your existing copy keeps working forever; new versions may\nrequire a new purchase. There is no subscription and no recurring charge of any\nkind.\n\n## Refunds\n\nBecause this is source code and delivery is immediate, a refund is available\nwithin 14 days of purchase if the software does not work as described. Run\n`npm test` before you ask — it takes a second and tells you whether the software\nis at fault.\n\n## Warranty and liability\n\nThe software is provided \"as is\", without warranty of any kind, express or\nimplied, including but not limited to the warranties of merchantability, fitness\nfor a particular purpose and non-infringement.\n\nIn no event shall [SELLER] be liable for any claim, damages or other liability,\nwhether in an action of contract, tort or otherwise, arising from, out of or in\nconnection with the software or its use.\n\nIn particular: this tool drafts a digest from your repository's own pull-request\ndata via GitHub's API. It does not review code, approve or block anything, or\nreplace a human reviewer — the digest is a starting point, not a final\njudgment. You are responsible for your own API keys, tokens, and their\nassociated costs and access scopes.\n\n## Governing law\n\nThis license is governed by the laws of [JURISDICTION].\n\n---\n\nCopyright © 2026 [SELLER]. All rights reserved.\n\nThe source files each carry the same notice. Copyright in this software arises\nautomatically on creation and is not conditional on registration, on this notice,\nor on any filing — the notice exists to make ownership unambiguous and to travel\nwith a file that gets separated from this license.\n",
  },
  {
    path: "package.json",
    contents: "{\n  \"name\": \"code-review-digest\",\n  \"version\": \"1.0.0\",\n  \"description\": \"A GitHub Actions workflow that posts a real, prioritized PR digest to Slack on a schedule — not a blueprint you wire yourself.\",\n  \"license\": \"SEE LICENSE IN LICENSE.md\",\n  \"type\": \"module\",\n  \"engines\": {\n    \"node\": \">=18\"\n  },\n  \"bin\": {\n    \"code-review-digest\": \"./bin/digest.mjs\"\n  },\n  \"main\": \"./lib/digest.mjs\",\n  \"files\": [\n    \"bin\",\n    \"lib\",\n    \".github-workflow-template\",\n    \"README.md\",\n    \"LICENSE.md\"\n  ],\n  \"scripts\": {\n    \"digest\": \"node bin/digest.mjs\",\n    \"test\": \"node test/run.mjs\"\n  },\n  \"dependencies\": {},\n  \"devDependencies\": {}\n}\n",
  },
  {
    path: ".env.example",
    contents: "# For local testing only — inside real GitHub Actions these are repo secrets,\n# not a file. Copy to .env and fill in for local runs.\n\nANTHROPIC_API_KEY=\nSLACK_WEBHOOK_URL=\n\n# Only needed to run bin/digest.mjs outside of Actions.\nGITHUB_TOKEN=\nGITHUB_REPOSITORY=\n",
  },
  {
    path: "lib/digest.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nconst MODEL = 'claude-sonnet-4-5'\nconst API_URL = 'https://api.anthropic.com/v1/messages'\n\nconst SYSTEM_PROMPT = `You write short, direct code-review digests for a Slack channel. Given a \\\nlist of open pull requests (title, author, age, last-updated, draft status, \\\nlatest review state), produce a digest that:\n- Leads with what actually needs a human today (stale, unreviewed, or\n  changes-requested-and-not-addressed PRs) — not a flat list in PR-number order.\n- Is honest when nothing is urgent — say so in one line, don't pad it.\n- Never invents information not present in the input.\n\nRespond with ONLY a JSON object, no markdown fences:\n{\n  \"headline\": string,               // one line, e.g. \"3 PRs need attention, 2 are fine to leave\"\n  \"needsAttention\": [{ \"number\": number, \"title\": string, \"author\": string, \"reason\": string, \"url\": string }],\n  \"fine\": [{ \"number\": number, \"title\": string, \"url\": string }]\n}`\n\n/**\n * @param {Array} prs   from listOpenPRs()\n * @param {object} [options]\n * @param {string} [options.apiKey]  defaults to ANTHROPIC_API_KEY\n */\nexport async function draftDigest(prs, options = {}) {\n  const apiKey = options.apiKey ?? process.env.ANTHROPIC_API_KEY\n  if (!apiKey) {\n    throw new Error('ANTHROPIC_API_KEY is not set — add it as a repo secret and pass it to the workflow step.')\n  }\n\n  if (prs.length === 0) {\n    return { headline: 'No open pull requests.', needsAttention: [], fine: [] }\n  }\n\n  const input = prs\n    .map(\n      (pr) =>\n        `#${pr.number} \"${pr.title}\" by ${pr.author} — opened ${pr.ageDays}d ago, updated ${pr.updatedDaysAgo}d ago, ` +\n        `${pr.draft ? 'draft, ' : ''}latest review: ${pr.reviewDecision ?? 'none'}`,\n    )\n    .join('\\n')\n\n  const response = await fetch(API_URL, {\n    method: 'POST',\n    headers: { 'content-type': 'application/json', 'x-api-key': apiKey, 'anthropic-version': '2023-06-01' },\n    body: JSON.stringify({\n      model: MODEL,\n      max_tokens: 1200,\n      system: SYSTEM_PROMPT,\n      messages: [{ role: 'user', content: `Open pull requests:\\n${input}` }],\n    }),\n    signal: AbortSignal.timeout(30_000),\n  })\n\n  if (!response.ok) {\n    const body = await response.text().catch(() => '')\n    throw new Error(`Claude API returned HTTP ${response.status}: ${body.slice(0, 300)}`)\n  }\n\n  const data = await response.json()\n  const text = data?.content?.[0]?.text\n  if (!text) throw new Error('Claude API returned no text content.')\n\n  try {\n    return JSON.parse(text)\n  } catch {\n    const match = text.match(/\\{[\\s\\S]*\\}/)\n    if (!match) throw new Error(`Could not parse a digest from the model's response: ${text.slice(0, 200)}`)\n    return JSON.parse(match[0])\n  }\n}\n",
  },
  {
    path: "lib/github.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Reads open pull requests for a repo via GitHub's REST API. Inside GitHub\n// Actions, GITHUB_TOKEN and GITHUB_REPOSITORY are already set — no extra\n// secret to create for this part. The token needs no scopes beyond what\n// Actions grants by default (contents: read, pull-requests: read) as long\n// as the workflow's `permissions:` block allows it — see the template.\n\nconst API = 'https://api.github.com'\n\nfunction repoSlug() {\n  const slug = process.env.GITHUB_REPOSITORY\n  if (!slug) throw new Error('GITHUB_REPOSITORY is not set — this is meant to run inside GitHub Actions.')\n  return slug\n}\n\nfunction authHeaders(token) {\n  const t = token ?? process.env.GITHUB_TOKEN\n  if (!t) throw new Error('GITHUB_TOKEN is not set. In a workflow it is provided automatically via ${{ secrets.GITHUB_TOKEN }}.')\n  return { Authorization: `Bearer ${t}`, Accept: 'application/vnd.github+json', 'X-GitHub-Api-Version': '2022-11-28' }\n}\n\n/**\n * @param {object} [options]\n * @param {string} [options.token]\n * @param {number} [options.staleDays=3]  a PR with no activity for this long is flagged stale\n * @returns {Promise<Array<{number:number, title:string, author:string, url:string,\n *   draft:boolean, reviewDecision:string|null, ageDays:number, updatedDaysAgo:number}>>}\n */\nexport async function listOpenPRs(options = {}) {\n  const staleDays = options.staleDays ?? 3\n  const repo = repoSlug()\n  const headers = authHeaders(options.token)\n\n  const res = await fetch(`${API}/repos/${repo}/pulls?state=open&per_page=50&sort=updated&direction=asc`, { headers })\n  if (!res.ok) throw new Error(`GitHub API returned HTTP ${res.status} listing PRs for ${repo}`)\n  const prs = await res.json()\n\n  const now = Date.now()\n  const withReviews = await Promise.all(\n    prs.map(async (pr) => {\n      let reviewDecision = null\n      try {\n        const reviewsRes = await fetch(`${API}/repos/${repo}/pulls/${pr.number}/reviews?per_page=20`, { headers })\n        if (reviewsRes.ok) {\n          const reviews = await reviewsRes.json()\n          const latest = reviews[reviews.length - 1]\n          reviewDecision = latest?.state ?? null\n        }\n      } catch {\n        // Best-effort — a digest missing one PR's review state is still useful.\n      }\n      const ageDays = Math.floor((now - new Date(pr.created_at).getTime()) / 86_400_000)\n      const updatedDaysAgo = Math.floor((now - new Date(pr.updated_at).getTime()) / 86_400_000)\n      return {\n        number: pr.number,\n        title: pr.title,\n        author: pr.user?.login ?? 'unknown',\n        url: pr.html_url,\n        draft: pr.draft ?? false,\n        reviewDecision,\n        ageDays,\n        updatedDaysAgo,\n        stale: updatedDaysAgo >= staleDays,\n      }\n    }),\n  )\n\n  return withReviews\n}\n",
  },
  {
    path: "lib/slack.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nfunction blocksFor(digest) {\n  const blocks = [\n    { type: 'header', text: { type: 'plain_text', text: 'Code review digest' } },\n    { type: 'section', text: { type: 'mrkdwn', text: digest.headline || '' } },\n  ]\n\n  if (digest.needsAttention?.length) {\n    const lines = digest.needsAttention\n      .map((pr) => `• <${pr.url}|#${pr.number} ${pr.title}> — ${pr.author} — _${pr.reason}_`)\n      .join('\\n')\n    blocks.push({ type: 'section', text: { type: 'mrkdwn', text: `*Needs attention*\\n${lines}` } })\n  }\n\n  if (digest.fine?.length) {\n    const lines = digest.fine.map((pr) => `• <${pr.url}|#${pr.number} ${pr.title}>`).join('\\n')\n    blocks.push({ type: 'section', text: { type: 'mrkdwn', text: `*Fine to leave*\\n${lines}` } })\n  }\n\n  return blocks\n}\n\n/**\n * @param {object} digest        from draftDigest()\n * @param {string|undefined} webhookUrl\n * @returns {Promise<boolean>}\n */\nexport async function postToSlack(digest, webhookUrl) {\n  if (!webhookUrl) return false\n  try {\n    const response = await fetch(webhookUrl, {\n      method: 'POST',\n      headers: { 'content-type': 'application/json' },\n      body: JSON.stringify({ text: digest.headline || 'Code review digest', blocks: blocksFor(digest) }),\n      signal: AbortSignal.timeout(8000),\n    })\n    if (!response.ok) {\n      console.warn(`Slack webhook returned HTTP ${response.status}`)\n      return false\n    }\n    return true\n  } catch (error) {\n    console.warn(`Slack webhook failed: ${error?.message ?? error}`)\n    return false\n  }\n}\n",
  },
  {
    path: "bin/digest.mjs",
    contents: "#!/usr/bin/env node\n// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// What .github-workflow-template/code-review-digest.yml actually runs. Reads\n// SLACK_WEBHOOK_URL and ANTHROPIC_API_KEY from the environment (set as repo\n// secrets in the workflow), GITHUB_TOKEN and GITHUB_REPOSITORY are already\n// present inside Actions.\n//\n// Also runnable by hand outside Actions, against any repo, for testing:\n//   GITHUB_TOKEN=ghp_... GITHUB_REPOSITORY=you/repo node bin/digest.mjs\n\nimport { listOpenPRs } from '../lib/github.mjs'\nimport { draftDigest } from '../lib/digest.mjs'\nimport { postToSlack } from '../lib/slack.mjs'\n\nfunction parseArgs(argv) {\n  const args = {}\n  for (const a of argv) {\n    if (a === '--version') args.version = true\n    if (a === '--json') args.json = true\n  }\n  return args\n}\n\nasync function main() {\n  const args = parseArgs(process.argv.slice(2))\n  if (args.version) {\n    console.log('code-review-digest 1.0.0')\n    return\n  }\n\n  console.error('Fetching open pull requests...')\n  const prs = await listOpenPRs()\n  console.error(`Found ${prs.length} open PR(s). Drafting digest...`)\n  const digest = await draftDigest(prs)\n\n  if (args.json) {\n    console.log(JSON.stringify(digest, null, 2))\n  } else {\n    console.log(`\\n${digest.headline}\\n`)\n    if (digest.needsAttention?.length) {\n      console.log('Needs attention:')\n      for (const pr of digest.needsAttention) console.log(`  #${pr.number} ${pr.title} (${pr.author}) — ${pr.reason}`)\n    }\n    if (digest.fine?.length) {\n      console.log('\\nFine to leave:')\n      for (const pr of digest.fine) console.log(`  #${pr.number} ${pr.title}`)\n    }\n  }\n\n  const posted = await postToSlack(digest, process.env.SLACK_WEBHOOK_URL)\n  console.error(posted ? '\\nPosted to Slack.' : '\\nSlack not configured or post failed — see warning above.')\n}\n\nmain().catch((err) => {\n  console.error(`Error: ${err.message}`)\n  process.exitCode = 1\n})\n",
  },
  {
    path: ".github-workflow-template/code-review-digest.yml",
    contents: "# Copy this file to .github/workflows/code-review-digest.yml in the repo you\n# want digests for (this exact path — GitHub only runs workflows from\n# .github/workflows/). Also copy the bin/ and lib/ folders somewhere in that\n# repo (e.g. into a tools/code-review-digest/ directory) and adjust the\n# `working-directory` / script path below to match where you put them.\n#\n# Two repo secrets required (Settings -> Secrets and variables -> Actions):\n#   ANTHROPIC_API_KEY   your own key from console.anthropic.com\n#   SLACK_WEBHOOK_URL   Slack -> Apps -> Incoming Webhooks\n#\n# GITHUB_TOKEN needs no secret — Actions provides it automatically. The\n# `permissions:` block below is the minimum this needs; GitHub defaults new\n# repos to read-only tokens already, but this makes the requirement explicit\n# rather than relying on an org-wide default that could change.\n\nname: Code review digest\n\non:\n  schedule:\n    - cron: '0 14 * * 1-5' # 14:00 UTC, weekdays — adjust to your team's morning\n  workflow_dispatch: {} # lets you trigger it manually from the Actions tab too\n\npermissions:\n  contents: read\n  pull-requests: read\n\njobs:\n  digest:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n\n      - name: Run digest\n        working-directory: tools/code-review-digest # <- adjust to wherever you put lib/ and bin/\n        env:\n          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}\n          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}\n        run: node bin/digest.mjs\n",
  },
  {
    path: "test/run.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nimport assert from 'node:assert/strict'\nimport { draftDigest } from '../lib/digest.mjs'\nimport { postToSlack } from '../lib/slack.mjs'\nimport { listOpenPRs } from '../lib/github.mjs'\n\nlet failures = 0\nasync function test(name, fn) {\n  try {\n    await fn()\n    console.log(`ok - ${name}`)\n  } catch (err) {\n    failures++\n    console.error(`not ok - ${name}`)\n    console.error(`  ${err.message}`)\n  }\n}\n\nawait test('draftDigest rejects a missing API key', async () => {\n  await assert.rejects(() => draftDigest([{ number: 1, title: 'x' }], { apiKey: '' }), /ANTHROPIC_API_KEY/)\n})\n\nawait test('draftDigest returns a clean empty digest with no PRs, no API call needed', async () => {\n  const digest = await draftDigest([], { apiKey: 'sk-test-not-real' })\n  assert.equal(digest.headline, 'No open pull requests.')\n  assert.deepEqual(digest.needsAttention, [])\n})\n\nawait test('postToSlack is a no-op with no webhook configured', async () => {\n  const posted = await postToSlack({ headline: 'x' }, undefined)\n  assert.equal(posted, false)\n})\n\nawait test('listOpenPRs rejects when GITHUB_REPOSITORY is not set', async () => {\n  const saved = process.env.GITHUB_REPOSITORY\n  delete process.env.GITHUB_REPOSITORY\n  try {\n    await assert.rejects(() => listOpenPRs(), /GITHUB_REPOSITORY/)\n  } finally {\n    if (saved) process.env.GITHUB_REPOSITORY = saved\n  }\n})\n\nconsole.log(failures === 0 ? '\\nAll checks passed.' : `\\n${failures} check(s) failed.`)\nprocess.exitCode = failures === 0 ? 0 : 1\n",
  },
]
