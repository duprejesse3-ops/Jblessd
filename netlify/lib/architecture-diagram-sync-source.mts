// GENERATED FILE — DO NOT EDIT BY HAND.
//
// Produced by packages/architecture-diagram-sync/tools/embed-source.mjs from the real
// package source. Regenerate after changing the package:
//
//   node packages/architecture-diagram-sync/tools/embed-source.mjs
//
// This is the payload for the Architecture Diagram Sync product (SKU AI-AB-034): the
// complete, runnable source the buyer receives at checkout. It is embedded
// rather than read from disk so fulfilment cannot fail on a missing file.

export interface SourceFile {
  path: string
  contents: string
}

export const ARCHITECTURE_DIAGRAM_SYNC_SOURCE: SourceFile[] = [
  {
    path: "README.md",
    contents: "# Architecture Diagram Sync\n\nA GitHub Actions workflow that regenerates `docs/architecture.md` — a real\nMermaid diagram — from your codebase's actual structure on every push to\nmain. Not a blueprint you wire yourself.\n\n## What it does, precisely\n\n1. On every push to `main`, it scans the repo: top-level directories, how\n   many source files each has, and which directories import from which\n   others (regex-based — no parser, no build step, works on plain JS/TS).\n2. Claude draws a Mermaid flowchart from that scan. It's instructed not to\n   invent a node or edge that isn't in the scan — the diagram is a picture\n   of what's actually there, not what an LLM assumes a repo like this\n   \"usually\" looks like.\n3. Writes `docs/architecture.md`. If the diagram is identical to what's\n   already committed, nothing happens — no empty commits on every merge.\n4. If it changed, commits it with `[skip ci]` (so the commit doesn't\n   re-trigger this same workflow and loop forever).\n\n## What it does not do\n\nParse your actual AST, understand TypeScript types, or follow path aliases\n(`@/lib/...` style imports) — the scan is regex-based on relative import\npaths (`./`, `../`) specifically so it has zero dependencies and needs no\nbuild step. If your codebase uses path aliases heavily, the cross-directory\nedges it finds will be incomplete. It also doesn't read Terraform/infra\nfiles for resource-level detail yet — it only notes which infra files exist\n(Dockerfile, docker-compose.yml, terraform/, etc.) as context for the\ndiagram, not as diagram nodes themselves.\n\n## Install (5 minutes)\n\n1. Copy `bin/` and `lib/` into your repo, e.g. `tools/architecture-diagram-sync/`.\n2. Copy `.github-workflow-template/architecture-diagram-sync.yml` to\n   `.github/workflows/architecture-diagram-sync.yml`.\n3. If you used a different location, edit `working-directory:` in the\n   workflow.\n4. Add one repo secret: `ANTHROPIC_API_KEY`.\n5. Push to main. First run creates `docs/architecture.md`; every push after\n   that keeps it honest.\n\nIf your default branch isn't `main`, update both the `on: push: branches:`\ntrigger and `git push origin HEAD:main` in the workflow.\n\n## Testing locally\n\n```sh\nnpm install --omit=dev\ncp .env.example .env\nnode bin/diagram-sync.mjs\ncat docs/architecture.md\n```\n\n```sh\nnpm test\n```\nRuns without real API keys — checks the scanner against a small fixture repo\nand the write-if-changed logic, not a live Claude call.\n",
  },
  {
    path: "LICENSE.md",
    contents: "# License\n\n**architecture-diagram-sync — perpetual single-purchase license**\n\n> This is a plain-language commercial license template. It has not been reviewed\n> by a lawyer. Have one look at it before you sell against it, and replace\n> `[SELLER]` and `[JURISDICTION]` with your details.\n\n## The short version\n\nYou bought it once. You own your copy forever. Run it against as many of **your own** repositories as you like. Do not\nresell it as a product of its own.\n\n## What you may do\n\n- Use the software for any purpose, commercial or personal, forever.\n- Run it on unlimited repositories that you own or operate.\n- Use it in client work, and use the diagrams it produces however you like\n  — including in customer-facing documentation.\n- Modify the source freely. It is plain JavaScript with no build step precisely so\n  that you can.\n- Keep using it indefinitely. There is no license key, no activation, no expiry,\n  no phone-home, and nothing that stops working if [SELLER] does.\n- Keep and use any version you have received, forever, regardless of what happens\n  to later versions or to [SELLER].\n\n## What you may not do\n\n- Resell, relicense, sublicense or redistribute the software itself, in whole or\n  in substantial part, as a product or as part of a product whose value is\n  substantially this software.\n- Publish the source publicly, or include it in a public repository, package\n  registry, or template that others can obtain without buying it.\n- Remove or alter this license file or the attribution in the source headers.\n\nSelling the *output* — architecture documentation, technical-writing\nservices — is explicitly allowed. Selling *the tool* is not.\n\n## Updates\n\nAny updates published within twelve months of your purchase are included at no\nextra cost. After that, your existing copy keeps working forever; new versions may\nrequire a new purchase. There is no subscription and no recurring charge of any\nkind.\n\n## Refunds\n\nBecause this is source code and delivery is immediate, a refund is available\nwithin 14 days of purchase if the software does not work as described. Run\n`npm test` before you ask — it takes a second and tells you whether the software\nis at fault.\n\n## Warranty and liability\n\nThe software is provided \"as is\", without warranty of any kind, express or\nimplied, including but not limited to the warranties of merchantability, fitness\nfor a particular purpose and non-infringement.\n\nIn no event shall [SELLER] be liable for any claim, damages or other liability,\nwhether in an action of contract, tort or otherwise, arising from, out of or in\nconnection with the software or its use.\n\nIn particular: this tool draws a diagram from a regex-based scan of your\nrepository's own source files, which is not a full parser and can miss or\nmisread unusual import patterns (dynamic requires, path aliases). It commits\nthe result to docs/architecture.md on your behalf. You are responsible for\nreviewing what it commits before treating it as authoritative, and for your\nown API keys and their associated costs.\n\n## Governing law\n\nThis license is governed by the laws of [JURISDICTION].\n\n---\n\nCopyright © 2026 [SELLER]. All rights reserved.\n\nThe source files each carry the same notice. Copyright in this software arises\nautomatically on creation and is not conditional on registration, on this notice,\nor on any filing — the notice exists to make ownership unambiguous and to travel\nwith a file that gets separated from this license.\n",
  },
  {
    path: "package.json",
    contents: "{\n  \"name\": \"architecture-diagram-sync\",\n  \"version\": \"1.0.0\",\n  \"description\": \"A GitHub Actions workflow that regenerates a real Mermaid architecture diagram from the actual codebase on every merge — not a blueprint you wire yourself.\",\n  \"license\": \"SEE LICENSE IN LICENSE.md\",\n  \"type\": \"module\",\n  \"engines\": {\n    \"node\": \">=18\"\n  },\n  \"bin\": {\n    \"diagram-sync\": \"./bin/diagram-sync.mjs\"\n  },\n  \"main\": \"./lib/diagram.mjs\",\n  \"files\": [\n    \"bin\",\n    \"lib\",\n    \".github-workflow-template\",\n    \"README.md\",\n    \"LICENSE.md\"\n  ],\n  \"scripts\": {\n    \"diagram-sync\": \"node bin/diagram-sync.mjs\",\n    \"test\": \"node test/run.mjs\"\n  },\n  \"dependencies\": {},\n  \"devDependencies\": {}\n}\n",
  },
  {
    path: ".env.example",
    contents: "# For local testing only — inside real GitHub Actions this is a repo secret.\nANTHROPIC_API_KEY=\n\n# Optional overrides — default to the current directory and ./docs/architecture.md\nREPO_ROOT=\nDIAGRAM_PATH=\n",
  },
  {
    path: "lib/diagram.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nconst MODEL = 'claude-sonnet-4-5'\nconst API_URL = 'https://api.anthropic.com/v1/messages'\n\nconst SYSTEM_PROMPT = `You draw a Mermaid flowchart of a codebase's real structure from a factual \\\nscan: top-level directories, how many files each has, which directories \\\nimport from which others (with counts), and which infra files exist. Every \\\nnode and edge in your diagram must correspond to something in the input —\ndo not invent a component, a connection, or a purpose that isn't there. If \\\na directory's purpose isn't obvious from its name, label the node with the \\\nname as-is rather than guessing what it does.\n\nRespond with ONLY a JSON object, no markdown fences:\n{\n  \"mermaid\": string,     // a complete \\`flowchart TD\\` diagram, ready to paste into a \\`\\`\\`mermaid block\n  \"notes\": string        // 2-4 sentences: what the diagram shows, any infra components noted separately\n}`\n\n/**\n * @param {object} scan   from scanRepo()\n * @param {object} [options]\n */\nexport async function draftDiagram(scan, options = {}) {\n  const apiKey = options.apiKey ?? process.env.ANTHROPIC_API_KEY\n  if (!apiKey) {\n    throw new Error('ANTHROPIC_API_KEY is not set — add it as a repo secret and pass it to the workflow step.')\n  }\n\n  const input = JSON.stringify(scan, null, 2)\n\n  const response = await fetch(API_URL, {\n    method: 'POST',\n    headers: { 'content-type': 'application/json', 'x-api-key': apiKey, 'anthropic-version': '2023-06-01' },\n    body: JSON.stringify({\n      model: MODEL,\n      max_tokens: 1500,\n      system: SYSTEM_PROMPT,\n      messages: [{ role: 'user', content: `Repo scan:\\n${input}` }],\n    }),\n    signal: AbortSignal.timeout(30_000),\n  })\n\n  if (!response.ok) {\n    const body = await response.text().catch(() => '')\n    throw new Error(`Claude API returned HTTP ${response.status}: ${body.slice(0, 300)}`)\n  }\n\n  const data = await response.json()\n  const text = data?.content?.[0]?.text\n  if (!text) throw new Error('Claude API returned no text content.')\n\n  try {\n    return JSON.parse(text)\n  } catch {\n    const match = text.match(/\\{[\\s\\S]*\\}/)\n    if (!match) throw new Error(`Could not parse a diagram from the model's response: ${text.slice(0, 200)}`)\n    return JSON.parse(match[0])\n  }\n}\n",
  },
  {
    path: "lib/scan.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Builds a factual summary of the repo's real structure: top-level\n// directories, which of them import from which others (regex-based, not a\n// full parser — deliberately, so this has zero dependencies and works on\n// any JS/TS codebase without a build step), and which infra files actually\n// exist. This is the input the diagram gets drawn from — nothing in the\n// diagram step is allowed to invent a component that isn't in this scan.\n\nimport { readdirSync, readFileSync, statSync, existsSync } from 'node:fs'\nimport { join, relative, dirname } from 'node:path'\n\nconst EXCLUDE_DIRS = new Set(['node_modules', '.git', 'dist', 'build', '.next', 'coverage', '.vercel', '.netlify'])\nconst CODE_EXT = new Set(['.js', '.jsx', '.ts', '.tsx', '.mjs', '.mts', '.cjs'])\nconst IMPORT_RE = /(?:import[\\s\\S]*?from\\s+|require\\()\\s*['\"](\\.[^'\"]+)['\"]/g\n\nconst INFRA_FILES = [\n  'Dockerfile',\n  'docker-compose.yml',\n  'netlify.toml',\n  'vercel.json',\n  '.github/workflows',\n  'terraform',\n  'infra',\n]\n\nfunction walk(dir, root, out = []) {\n  for (const entry of readdirSync(dir)) {\n    if (EXCLUDE_DIRS.has(entry) || entry.startsWith('.')) continue\n    const full = join(dir, entry)\n    const stat = statSync(full)\n    if (stat.isDirectory()) walk(full, root, out)\n    else out.push(full)\n  }\n  return out\n}\n\nfunction topLevelDir(root, filePath) {\n  const rel = relative(root, filePath)\n  const first = rel.split('/')[0]\n  return first || '.'\n}\n\n/**\n * @param {string} root   repo root to scan, defaults to cwd\n * @returns {{\n *   topLevelDirs: string[],\n *   fileCounts: Record<string, number>,\n *   crossDirImports: Array<{from:string, to:string, count:number}>,\n *   infra: string[],\n * }}\n */\nexport function scanRepo(root = process.cwd()) {\n  const files = walk(root, root).filter((f) => CODE_EXT.has('.' + f.split('.').pop()))\n\n  const fileCounts = {}\n  const edgeCounts = new Map() // \"from->to\" -> count\n\n  for (const file of files) {\n    const dir = topLevelDir(root, file)\n    fileCounts[dir] = (fileCounts[dir] ?? 0) + 1\n\n    let contents\n    try {\n      contents = readFileSync(file, 'utf8')\n    } catch {\n      continue\n    }\n\n    for (const match of contents.matchAll(IMPORT_RE)) {\n      const resolved = join(dirname(file), match[1])\n      const targetDir = topLevelDir(root, resolved)\n      if (targetDir === dir) continue // same top-level dir, not a cross-boundary edge\n      const key = `${dir}->${targetDir}`\n      edgeCounts.set(key, (edgeCounts.get(key) ?? 0) + 1)\n    }\n  }\n\n  const crossDirImports = [...edgeCounts.entries()].map(([key, count]) => {\n    const [from, to] = key.split('->')\n    return { from, to, count }\n  })\n\n  const infra = INFRA_FILES.filter((f) => existsSync(join(root, f)))\n\n  return {\n    topLevelDirs: Object.keys(fileCounts).sort(),\n    fileCounts,\n    crossDirImports: crossDirImports.sort((a, b) => b.count - a.count),\n    infra,\n  }\n}\n",
  },
  {
    path: "lib/write.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nimport { readFileSync, writeFileSync, existsSync, mkdirSync } from 'node:fs'\nimport { dirname } from 'node:path'\n\n/**\n * @param {string} path       usually 'docs/architecture.md'\n * @param {object} draft      from draftDiagram(): { mermaid, notes }\n * @returns {boolean} true if the file's content actually changed\n */\nexport function writeDiagram(path, draft) {\n  const content =\n    `# Architecture\\n\\n` +\n    `_Regenerated automatically from the codebase's real structure — see ` +\n    `[architecture-diagram-sync](https://github.com) in this repo's workflows. ` +\n    `Do not hand-edit; the next merge to main overwrites this file._\\n\\n` +\n    `${draft.notes}\\n\\n` +\n    '```mermaid\\n' +\n    draft.mermaid.trim() +\n    '\\n```\\n'\n\n  const previous = existsSync(path) ? readFileSync(path, 'utf8') : null\n  if (previous === content) return false\n\n  mkdirSync(dirname(path), { recursive: true })\n  writeFileSync(path, content)\n  return true\n}\n",
  },
  {
    path: "bin/diagram-sync.mjs",
    contents: "#!/usr/bin/env node\n// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// What .github-workflow-template/architecture-diagram-sync.yml runs on every\n// push to main. Prints whether the diagram actually changed, via exit code\n// and a line on stdout — the workflow uses that to skip an empty commit.\n\nimport { scanRepo } from '../lib/scan.mjs'\nimport { draftDiagram } from '../lib/diagram.mjs'\nimport { writeDiagram } from '../lib/write.mjs'\n\nasync function main() {\n  const root = process.env.REPO_ROOT || process.cwd()\n  const outPath = process.env.DIAGRAM_PATH || 'docs/architecture.md'\n\n  console.error(`Scanning ${root}...`)\n  const scan = scanRepo(root)\n  console.error(`Found ${scan.topLevelDirs.length} top-level dirs, ${scan.crossDirImports.length} cross-dir import edges.`)\n\n  console.error('Drafting diagram...')\n  const draft = await draftDiagram(scan)\n\n  const changed = writeDiagram(outPath, draft)\n  console.log(changed ? 'changed' : 'unchanged')\n  console.error(changed ? `Wrote ${outPath}.` : `${outPath} already matches the current structure — nothing to commit.`)\n}\n\nmain().catch((err) => {\n  console.error(`Error: ${err.message}`)\n  process.exitCode = 1\n})\n",
  },
  {
    path: ".github-workflow-template/architecture-diagram-sync.yml",
    contents: "# Copy to .github/workflows/architecture-diagram-sync.yml. Copy bin/ and\n# lib/ somewhere in the repo too (adjust `working-directory` below).\n#\n# Runs on every push to main, scans the repo's real structure, and\n# regenerates docs/architecture.md. Commits only if the diagram actually\n# changed — a push that doesn't touch cross-directory imports produces no\n# commit, so this doesn't create noise on every merge.\n#\n# One secret required:\n#   ANTHROPIC_API_KEY   your own key from console.anthropic.com\n\nname: Architecture diagram sync\n\non:\n  push:\n    branches: [main]\n\npermissions:\n  contents: write\n\njobs:\n  diagram-sync:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n\n      - name: Regenerate diagram\n        id: sync\n        working-directory: tools/architecture-diagram-sync # <- adjust to wherever you put lib/ and bin/\n        env:\n          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}\n          REPO_ROOT: ${{ github.workspace }}\n          DIAGRAM_PATH: ${{ github.workspace }}/docs/architecture.md\n        run: |\n          RESULT=$(node bin/diagram-sync.mjs)\n          echo \"result=${RESULT}\" >> \"$GITHUB_OUTPUT\"\n\n      - name: Commit if changed\n        if: steps.sync.outputs.result == 'changed'\n        run: |\n          git config user.name \"architecture-diagram-sync\"\n          git config user.email \"actions@users.noreply.github.com\"\n          git add docs/architecture.md\n          git commit -m \"docs: regenerate architecture diagram [skip ci]\"\n          git push origin HEAD:main\n",
  },
  {
    path: "test/run.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nimport assert from 'node:assert/strict'\nimport { mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs'\nimport { tmpdir } from 'node:os'\nimport { join } from 'node:path'\nimport { scanRepo } from '../lib/scan.mjs'\nimport { draftDiagram } from '../lib/diagram.mjs'\nimport { writeDiagram } from '../lib/write.mjs'\n\nlet failures = 0\nasync function test(name, fn) {\n  try {\n    await fn()\n    console.log(`ok - ${name}`)\n  } catch (err) {\n    failures++\n    console.error(`not ok - ${name}`)\n    console.error(`  ${err.message}`)\n  }\n}\n\nfunction buildFixtureRepo() {\n  const root = mkdtempSync(join(tmpdir(), 'diagram-sync-test-'))\n  mkdirSync(join(root, 'lib'), { recursive: true })\n  mkdirSync(join(root, 'api'), { recursive: true })\n  writeFileSync(join(root, 'lib', 'core.mjs'), 'export const x = 1\\n')\n  writeFileSync(join(root, 'api', 'handler.mjs'), \"import { x } from '../lib/core.mjs'\\nexport default x\\n\")\n  writeFileSync(join(root, 'Dockerfile'), 'FROM node:20\\n')\n  return root\n}\n\nawait test('scanRepo finds real top-level dirs and a real cross-dir import edge', () => {\n  const root = buildFixtureRepo()\n  try {\n    const scan = scanRepo(root)\n    assert.deepEqual(scan.topLevelDirs, ['api', 'lib'])\n    assert.equal(scan.fileCounts.api, 1)\n    assert.equal(scan.fileCounts.lib, 1)\n    const edge = scan.crossDirImports.find((e) => e.from === 'api' && e.to === 'lib')\n    assert.ok(edge, 'expected an api -> lib import edge')\n    assert.ok(scan.infra.includes('Dockerfile'))\n  } finally {\n    rmSync(root, { recursive: true, force: true })\n  }\n})\n\nawait test('draftDiagram rejects a missing API key', async () => {\n  await assert.rejects(\n    () => draftDiagram({ topLevelDirs: [], fileCounts: {}, crossDirImports: [], infra: [] }, { apiKey: '' }),\n    /ANTHROPIC_API_KEY/,\n  )\n})\n\nawait test('writeDiagram reports changed on first write, unchanged on identical rewrite', () => {\n  const dir = mkdtempSync(join(tmpdir(), 'diagram-sync-write-'))\n  const path = join(dir, 'docs', 'architecture.md')\n  const draft = { mermaid: 'flowchart TD\\n  a --> b', notes: 'test' }\n  try {\n    const first = writeDiagram(path, draft)\n    assert.equal(first, true)\n    const second = writeDiagram(path, draft)\n    assert.equal(second, false)\n  } finally {\n    rmSync(dir, { recursive: true, force: true })\n  }\n})\n\nconsole.log(failures === 0 ? '\\nAll checks passed.' : `\\n${failures} check(s) failed.`)\nprocess.exitCode = failures === 0 ? 0 : 1\n",
  },
]
