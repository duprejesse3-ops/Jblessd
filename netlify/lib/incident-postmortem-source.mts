// GENERATED FILE — DO NOT EDIT BY HAND.
//
// Produced by packages/incident-postmortem-automation/tools/embed-source.mjs from the real
// package source. Regenerate after changing the package:
//
//   node packages/incident-postmortem-automation/tools/embed-source.mjs
//
// This is the payload for the Incident Postmortem Automation product (SKU AI-AB-037): the
// complete, runnable source the buyer receives at checkout. It is embedded
// rather than read from disk so fulfilment cannot fail on a missing file.

export interface SourceFile {
  path: string
  contents: string
}

export const INCIDENT_POSTMORTEM_SOURCE: SourceFile[] = [
  {
    path: "README.md",
    contents: "# Incident Postmortem Automation\n\nTurns an incident timeline into a blameless postmortem draft — root cause,\nimpact, and action items with an owner already assigned to each one. Two ways\nto use it:\n\n- **`postmortem` CLI** — point it at a timeline file, get a draft back.\n  Nothing to deploy.\n- **PagerDuty webhook** — wire it to `incident.resolved` and it drafts and\n  posts to Slack on its own, no one has to run a command.\n\nBoth call Claude once per incident using **your own** `ANTHROPIC_API_KEY` —\nbilled to your account, nothing routed through the seller.\n\n## Quick start (no PagerDuty needed)\n\n```sh\ncp .env.example .env    # fill in ANTHROPIC_API_KEY at minimum\nnpm install --omit=dev  # no dependencies today, but future-proof\nnode bin/postmortem.mjs your-timeline.txt\n```\n\n`your-timeline.txt` can be anything — a PagerDuty timeline export, a pasted\nSlack thread, or plain notes. There's no required format; Claude reads it as\nprose.\n\nPost straight to Slack instead of printing:\n\n```sh\nnode bin/postmortem.mjs your-timeline.txt --slack-webhook https://hooks.slack.com/services/...\n```\n\n## Running it as a real automation\n\nThis is the part that makes it an automation and not a blueprint: point\nPagerDuty at a webhook, and a draft appears in Slack the moment an incident\nresolves.\n\n1. **PagerDuty → Integrations → Generic Webhooks (V3)** → add an endpoint,\n   subscribed to **Incident Resolved**.\n2. Copy the signing secret PagerDuty shows you into `PAGERDUTY_WEBHOOK_SECRET`.\n3. **My Profile → User Settings → API Access** → create a read-only API\n   token, into `PAGERDUTY_API_TOKEN`. The webhook payload itself only\n   contains the incident summary — this token is what lets the tool pull the\n   actual timeline of log entries.\n4. **Slack → your workspace → Apps → Incoming Webhooks** → create one, into\n   `SLACK_WEBHOOK_URL`.\n\nThen pick one way to run it:\n\n**Your own server or Pi** (works well alongside Meridian Host, if you're\nalready running that):\n```sh\nnode adapters/pagerduty-webhook.mjs\n```\nPagerDuty needs a public HTTPS URL to reach this. A tunnel (`cloudflared`,\n`ngrok`) works for testing; put it behind Caddy/nginx for real use.\n\n**Netlify Function** — no machine to keep running:\n1. Copy `adapters/netlify-function.mts` into your own site's\n   `netlify/functions/` directory.\n2. Set the four environment variables in Netlify's site settings.\n3. Point PagerDuty at\n   `https://your-site.netlify.app/.netlify/functions/pagerduty-postmortem`.\n\n## What it actually does, precisely\n\n1. PagerDuty sends a webhook the moment an incident is marked resolved.\n2. The signature is verified (HMAC-SHA256 against `PAGERDUTY_WEBHOOK_SECRET`)\n   before anything else happens — an unverified request is rejected, not\n   processed.\n3. The incident's log entries are pulled from PagerDuty's API — the real\n   timeline of what happened, not just the title.\n4. Claude drafts a blameless postmortem: summary, root cause, timeline,\n   and action items, each with an owner and a due date.\n5. The draft is posted to Slack as a formatted message with a real checklist.\n\nWhat it does **not** do: fix the underlying issue, create tickets in Jira/\nLinear automatically, or page anyone. If you want ticket creation added,\n`lib/postmortem.mjs` returns a plain JS object — wiring it to your tracker's\nAPI from there is a small, contained addition, not a rewrite.\n\n## Testing your setup\n\n```sh\nnpm test\n```\n\nRuns without needing real API keys — it checks the JSON shape the drafting\nfunction is expected to produce and that the Slack formatter handles an empty\n`actionItems` list without crashing, so a broken environment fails fast and\nloud instead of silently drafting nothing.\n\nTo test the whole path with a real Claude call:\n```sh\nnode bin/postmortem.mjs test/fixtures/sample-timeline.txt --json\n```\n",
  },
  {
    path: "LICENSE.md",
    contents: "# License\n\n**incident-postmortem-automation — perpetual single-purchase license**\n\n> This is a plain-language commercial license template. It has not been reviewed\n> by a lawyer. Have one look at it before you sell against it, and replace\n> `[SELLER]` and `[JURISDICTION]` with your details.\n\n## The short version\n\nYou bought it once. You own your copy forever. Run it against as many of\n**your own** incidents, teams, and PagerDuty/Slack accounts as you like. Do\nnot resell it as a product of its own.\n\n## What you may do\n\n- Use the software for any purpose, commercial or personal, forever.\n- Run it for unlimited incidents, teams, and services that you own or operate.\n- Use it in client work, and use the postmortems it drafts however you like.\n- Modify the source freely. It is plain JavaScript with no build step precisely so\n  that you can.\n- Keep using it indefinitely. There is no license key, no activation, no expiry,\n  no phone-home, and nothing that stops working if [SELLER] does.\n- Keep and use any version you have received, forever, regardless of what happens\n  to later versions or to [SELLER].\n\n## What you may not do\n\n- Resell, relicense, sublicense or redistribute the software itself, in whole or\n  in substantial part, as a product or as part of a product whose value is\n  substantially this software.\n- Publish the source publicly, or include it in a public repository, package\n  registry, or template that others can obtain without buying it.\n- Remove or alter this license file or the attribution in the source headers.\n\nSelling the *output* — postmortem reports, incident-review retainers,\nreliability consulting — is explicitly allowed. Selling *the tool* is not.\n\n## Updates\n\nAny updates published within twelve months of your purchase are included at no\nextra cost. After that, your existing copy keeps working forever; new versions may\nrequire a new purchase. There is no subscription and no recurring charge of any\nkind.\n\n## Refunds\n\nBecause this is source code and delivery is immediate, a refund is available\nwithin 14 days of purchase if the software does not work as described. Run\n`npm test` before you ask — it takes a second and tells you whether the software\nis at fault.\n\n## Warranty and liability\n\nThe software is provided \"as is\", without warranty of any kind, express or\nimplied, including but not limited to the warranties of merchantability, fitness\nfor a particular purpose and non-infringement.\n\nIn no event shall [SELLER] be liable for any claim, damages or other liability,\nwhether in an action of contract, tort or otherwise, arising from, out of or in\nconnection with the software or its use.\n\nIn particular: this tool drafts a postmortem from data you provide or that your\nown PagerDuty/Slack integrations supply. It does not diagnose, fix, or prevent\nincidents, and the draft it produces is a starting point for a human to review,\nnot a final report. You are responsible for verifying its accuracy before\ntreating it as authoritative, and for your own API keys, tokens, and their\nassociated costs and access scopes.\n\n## Governing law\n\nThis license is governed by the laws of [JURISDICTION].\n\n---\n\nCopyright © 2026 [SELLER]. All rights reserved.\n\nThe source files each carry the same notice. Copyright in this software arises\nautomatically on creation and is not conditional on registration, on this notice,\nor on any filing — the notice exists to make ownership unambiguous and to travel\nwith a file that gets separated from this license.\n",
  },
  {
    path: "package.json",
    contents: "{\n  \"name\": \"incident-postmortem-automation\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Turns an incident timeline into a blameless postmortem draft with owned action items, automatically — a PagerDuty webhook, not a prompt you paste.\",\n  \"license\": \"SEE LICENSE IN LICENSE.md\",\n  \"type\": \"module\",\n  \"engines\": {\n    \"node\": \">=18\"\n  },\n  \"bin\": {\n    \"postmortem\": \"./bin/postmortem.mjs\"\n  },\n  \"main\": \"./lib/postmortem.mjs\",\n  \"exports\": {\n    \".\": \"./lib/postmortem.mjs\",\n    \"./slack\": \"./lib/slack.mjs\"\n  },\n  \"files\": [\n    \"bin\",\n    \"lib\",\n    \"adapters\",\n    \"README.md\",\n    \"LICENSE.md\"\n  ],\n  \"scripts\": {\n    \"postmortem\": \"node bin/postmortem.mjs\",\n    \"webhook\": \"node adapters/pagerduty-webhook.mjs\",\n    \"test\": \"node test/run.mjs\"\n  },\n  \"dependencies\": {},\n  \"devDependencies\": {}\n}\n",
  },
  {
    path: "install.sh",
    contents: "#!/bin/sh\n# Copyright (c) 2026 [SELLER]. All rights reserved.\n# Licensed to a single purchaser under the terms in LICENSE.md.\n# Redistribution or resale of this source, in whole or in part, is not permitted.\n\n# One-command install for the Incident Postmortem Automation.\n#\n#   ./install.sh\n#\n# Copies the package somewhere stable and puts a `postmortem` command on your\n# PATH. Nothing is downloaded, nothing is compiled, and no packages are\n# fetched from a registry — the whole tool is the files sitting next to this\n# script.\n#\n# Override either location if the defaults don't suit you:\n#   PREFIX=/usr/local ./install.sh     # system-wide (needs sudo)\n#   BIN_DIR=~/bin ./install.sh         # just the symlink somewhere else\n\nset -eu\n\nPACKAGE_DIR=$(CDPATH= cd -- \"$(dirname -- \"$0\")\" && pwd -P)\n\nPREFIX=\"${PREFIX:-$HOME/.local}\"\nLIB_DIR=\"${LIB_DIR:-$PREFIX/lib/incident-postmortem-automation}\"\nBIN_DIR=\"${BIN_DIR:-$PREFIX/bin}\"\n\n# ---- checks ----------------------------------------------------------------\n\nif ! command -v node >/dev/null 2>&1; then\n  echo \"Error: Node.js is not installed, or not on your PATH.\" >&2\n  echo \"This tool needs Node 18 or newer: https://nodejs.org/\" >&2\n  exit 1\nfi\n\nNODE_MAJOR=$(node -p 'process.versions.node.split(\".\")[0]' 2>/dev/null || echo 0)\nif [ \"$NODE_MAJOR\" -lt 18 ]; then\n  echo \"Error: Node $(node -v) is too old — this needs Node 18 or newer.\" >&2\n  echo \"Node 18 is where global fetch() landed, which this tool relies on.\" >&2\n  exit 1\nfi\n\nif [ ! -f \"$PACKAGE_DIR/bin/postmortem.mjs\" ]; then\n  echo \"Error: run this from inside the unpacked package — bin/postmortem.mjs is missing.\" >&2\n  exit 1\nfi\n\n# ---- install -----------------------------------------------------------------\n\necho \"Installing Incident Postmortem Automation\"\necho \"  from: $PACKAGE_DIR\"\necho \"  to:   $LIB_DIR\"\n\nmkdir -p \"$LIB_DIR\" \"$BIN_DIR\"\n\nfor entry in lib bin adapters test README.md LICENSE.md package.json install.sh .env.example; do\n  if [ -e \"$PACKAGE_DIR/$entry\" ]; then\n    rm -rf \"$LIB_DIR/$entry\"\n    cp -R \"$PACKAGE_DIR/$entry\" \"$LIB_DIR/$entry\"\n  fi\ndone\n\nchmod +x \"$LIB_DIR/bin/postmortem.mjs\" \"$LIB_DIR/adapters/pagerduty-webhook.mjs\" \"$LIB_DIR/install.sh\" 2>/dev/null || true\n\nln -sf \"$LIB_DIR/bin/postmortem.mjs\" \"$BIN_DIR/postmortem\"\n\n# ---- verify ------------------------------------------------------------------\n\nif ! \"$BIN_DIR/postmortem\" --version >/dev/null 2>&1; then\n  echo \"Error: installed, but 'postmortem --version' did not run cleanly.\" >&2\n  echo \"Try running it directly to see why: $LIB_DIR/bin/postmortem.mjs --version\" >&2\n  exit 1\nfi\n\nVERSION=$(\"$BIN_DIR/postmortem\" --version 2>/dev/null || echo '?')\n\necho\necho \"Installed $VERSION\"\necho\n\ncase \":${PATH}:\" in\n  *\":$BIN_DIR:\"*)\n    echo \"Try it:\"\n    echo \"  cp $LIB_DIR/.env.example $LIB_DIR/.env   # fill in ANTHROPIC_API_KEY\"\n    echo \"  postmortem your-timeline.txt\"\n    ;;\n  *)\n    echo \"One more step — $BIN_DIR is not on your PATH.\"\n    echo \"Add this to your ~/.profile, ~/.bashrc, or ~/.zshrc:\"\n    echo\n    echo \"  export PATH=\\\"$BIN_DIR:\\$PATH\\\"\"\n    echo\n    echo \"Or run it by full path right now:\"\n    echo \"  $BIN_DIR/postmortem your-timeline.txt\"\n    ;;\nesac\n\necho\necho \"Wire it to PagerDuty so it runs itself (see README.md \\\"Running it as a real automation\\\"):\"\necho \"  $LIB_DIR/adapters/pagerduty-webhook.mjs        # standalone server\"\necho \"  $LIB_DIR/adapters/netlify-function.mts         # serverless\"\necho\necho \"Uninstall:\"\necho \"  rm -rf \\\"$LIB_DIR\\\" \\\"$BIN_DIR/postmortem\\\"\"\n",
  },
  {
    path: "lib/postmortem.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// The actual automation, not a prompt to paste somewhere: given a timeline of\n// what happened during an incident, this calls Claude once and returns a\n// structured, blameless postmortem draft — root cause, impact, and action\n// items with an owner already assigned to each one, not just a paragraph you\n// still have to turn into tickets yourself.\n\nconst MODEL = 'claude-sonnet-4-5'\nconst API_URL = 'https://api.anthropic.com/v1/messages'\n\nconst SYSTEM_PROMPT = `You write blameless incident postmortems. Blameless means: describe what \\\nthe system and process allowed to happen, never \"who caused it.\" Every action \\\nitem must have a concrete owner (a role or team, e.g. \"on-call engineer\", \\\n\"platform team\" — a real name if one appears in the timeline) and be phrased \\\nas a specific, checkable task, not \"improve monitoring.\"\n\nRespond with ONLY a JSON object, no markdown fences, no commentary, matching \\\nexactly this shape:\n{\n  \"title\": string,\n  \"summary\": string,           // 2-3 sentences, what happened and impact\n  \"timeline\": [{ \"time\": string, \"event\": string }],\n  \"rootCause\": string,\n  \"actionItems\": [{ \"owner\": string, \"task\": string, \"dueBy\": string }]\n}`\n\n/**\n * @param {string} timelineText   raw incident timeline — paste from PagerDuty,\n *   a Slack thread export, or plain notes. No fixed format required.\n * @param {object} [options]\n * @param {string} [options.incidentTitle]\n * @param {string} [options.apiKey]  defaults to ANTHROPIC_API_KEY\n * @returns {Promise<{title:string, summary:string, timeline:Array, rootCause:string, actionItems:Array}>}\n */\nexport async function draftPostmortem(timelineText, options = {}) {\n  const apiKey = options.apiKey ?? process.env.ANTHROPIC_API_KEY\n  if (!apiKey) {\n    throw new Error(\n      'ANTHROPIC_API_KEY is not set. Get one at console.anthropic.com and put it in .env — ' +\n        'this is your own key, billed to your own account, never shared with the seller.',\n    )\n  }\n  if (!timelineText || !timelineText.trim()) {\n    throw new Error('No timeline text given — nothing to draft a postmortem from.')\n  }\n\n  const userMessage = options.incidentTitle\n    ? `Incident: ${options.incidentTitle}\\n\\nTimeline:\\n${timelineText}`\n    : `Timeline:\\n${timelineText}`\n\n  const response = await fetch(API_URL, {\n    method: 'POST',\n    headers: {\n      'content-type': 'application/json',\n      'x-api-key': apiKey,\n      'anthropic-version': '2023-06-01',\n    },\n    body: JSON.stringify({\n      model: MODEL,\n      max_tokens: 1500,\n      system: SYSTEM_PROMPT,\n      messages: [{ role: 'user', content: userMessage }],\n    }),\n    signal: AbortSignal.timeout(30_000),\n  })\n\n  if (!response.ok) {\n    const body = await response.text().catch(() => '')\n    throw new Error(`Claude API returned HTTP ${response.status}: ${body.slice(0, 300)}`)\n  }\n\n  const data = await response.json()\n  const text = data?.content?.[0]?.text\n  if (!text) throw new Error('Claude API returned no text content.')\n\n  let draft\n  try {\n    draft = JSON.parse(text)\n  } catch {\n    // Models occasionally wrap JSON in a fence despite instructions not to.\n    const match = text.match(/\\{[\\s\\S]*\\}/)\n    if (!match) throw new Error(`Could not parse a postmortem draft from the model's response: ${text.slice(0, 200)}`)\n    draft = JSON.parse(match[0])\n  }\n\n  if (!Array.isArray(draft.actionItems)) draft.actionItems = []\n  if (!Array.isArray(draft.timeline)) draft.timeline = []\n  return draft\n}\n",
  },
  {
    path: "lib/slack.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Posts a drafted postmortem to Slack as a real, readable message — action\n// items as a checklist with owners, not a wall of JSON. Uses an incoming\n// webhook URL (Slack: your workspace → Apps → Incoming Webhooks), the same\n// zero-dependency pattern as site-audit-agent/lib/notify.mjs: silent when no\n// webhook is configured, never throws into the caller.\n\nfunction blocksFor(draft) {\n  const blocks = [\n    { type: 'header', text: { type: 'plain_text', text: draft.title || 'Incident postmortem (draft)' } },\n    { type: 'section', text: { type: 'mrkdwn', text: draft.summary || '' } },\n  ]\n\n  if (draft.rootCause) {\n    blocks.push({ type: 'section', text: { type: 'mrkdwn', text: `*Root cause*\\n${draft.rootCause}` } })\n  }\n\n  if (draft.timeline?.length) {\n    const lines = draft.timeline.slice(0, 15).map((t) => `• \\`${t.time}\\` — ${t.event}`).join('\\n')\n    blocks.push({ type: 'section', text: { type: 'mrkdwn', text: `*Timeline*\\n${lines}` } })\n  }\n\n  if (draft.actionItems?.length) {\n    const lines = draft.actionItems\n      .map((a) => `• [ ] *${a.owner}* — ${a.task}${a.dueBy ? ` _(by ${a.dueBy})_` : ''}`)\n      .join('\\n')\n    blocks.push({ type: 'section', text: { type: 'mrkdwn', text: `*Action items*\\n${lines}` } })\n  }\n\n  blocks.push({\n    type: 'context',\n    elements: [{ type: 'mrkdwn', text: 'Drafted automatically — review before treating this as final.' }],\n  })\n\n  return blocks\n}\n\n/**\n * @param {object} draft          a draft from draftPostmortem()\n * @param {string|undefined} webhookUrl\n * @returns {Promise<boolean>} true if Slack accepted the message\n */\nexport async function postToSlack(draft, webhookUrl) {\n  if (!webhookUrl) return false\n  try {\n    const response = await fetch(webhookUrl, {\n      method: 'POST',\n      headers: { 'content-type': 'application/json' },\n      body: JSON.stringify({ text: draft.title || 'Incident postmortem (draft)', blocks: blocksFor(draft) }),\n      signal: AbortSignal.timeout(8000),\n    })\n    if (!response.ok) {\n      console.warn(`Slack webhook returned HTTP ${response.status}`)\n      return false\n    }\n    return true\n  } catch (error) {\n    console.warn(`Slack webhook failed: ${error?.message ?? error}`)\n    return false\n  }\n}\n",
  },
  {
    path: "bin/postmortem.mjs",
    contents: "#!/usr/bin/env node\n// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Usage:\n//   postmortem timeline.txt\n//   postmortem timeline.txt --title \"Checkout outage\" --slack-webhook https://hooks.slack.com/...\n//   cat timeline.txt | postmortem -\n//\n// Works with nothing but a text file and ANTHROPIC_API_KEY — the PagerDuty\n// webhook (adapters/pagerduty-webhook.mjs) is for when you want this to run\n// itself on every incident; this CLI is for running it on one incident right\n// now, or for testing your setup before wiring the webhook at all.\n\nimport { readFileSync } from 'node:fs'\nimport { draftPostmortem } from '../lib/postmortem.mjs'\nimport { postToSlack } from '../lib/slack.mjs'\n\nfunction parseArgs(argv) {\n  const args = { _: [] }\n  for (let i = 0; i < argv.length; i++) {\n    const a = argv[i]\n    if (a === '--title') args.title = argv[++i]\n    else if (a === '--slack-webhook') args.slackWebhook = argv[++i]\n    else if (a === '--json') args.json = true\n    else if (a === '--version') args.version = true\n    else args._.push(a)\n  }\n  return args\n}\n\nasync function main() {\n  const args = parseArgs(process.argv.slice(2))\n\n  if (args.version) {\n    console.log('incident-postmortem-automation 1.0.0')\n    return\n  }\n\n  const source = args._[0]\n  if (!source) {\n    console.error('Usage: postmortem <timeline-file | -> [--title \"...\"] [--slack-webhook URL] [--json]')\n    process.exitCode = 1\n    return\n  }\n\n  const timelineText = source === '-' ? readFileSync(0, 'utf8') : readFileSync(source, 'utf8')\n\n  console.error('Drafting postmortem...')\n  const draft = await draftPostmortem(timelineText, { incidentTitle: args.title })\n\n  if (args.json) {\n    console.log(JSON.stringify(draft, null, 2))\n  } else {\n    console.log(`\\n# ${draft.title}\\n`)\n    console.log(draft.summary + '\\n')\n    console.log('## Root cause\\n' + draft.rootCause + '\\n')\n    console.log('## Timeline')\n    for (const t of draft.timeline) console.log(`- ${t.time} — ${t.event}`)\n    console.log('\\n## Action items')\n    for (const a of draft.actionItems) console.log(`- [ ] ${a.owner} — ${a.task}${a.dueBy ? ` (by ${a.dueBy})` : ''}`)\n  }\n\n  const webhook = args.slackWebhook ?? process.env.SLACK_WEBHOOK_URL\n  if (webhook) {\n    const posted = await postToSlack(draft, webhook)\n    console.error(posted ? '\\nPosted to Slack.' : '\\nSlack post failed — see warning above.')\n  }\n}\n\nmain().catch((err) => {\n  console.error(`Error: ${err.message}`)\n  process.exitCode = 1\n})\n",
  },
  {
    path: "adapters/netlify-function.mts",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Same logic as adapters/pagerduty-webhook.mjs, as a Netlify Function — no\n// machine to keep running, no tunnel needed to give PagerDuty a public URL.\n// Drop this file into your own site's netlify/functions/ directory (rename\n// or keep as pagerduty-postmortem.mts) and point PagerDuty's webhook at\n// https://your-site.netlify.app/.netlify/functions/pagerduty-postmortem\n//\n// Requires the same three environment variables as the standalone server —\n// set them in Netlify's site settings, not in this file:\n//   PAGERDUTY_WEBHOOK_SECRET, PAGERDUTY_API_TOKEN, SLACK_WEBHOOK_URL,\n//   ANTHROPIC_API_KEY\n\nimport type { Config } from '@netlify/functions'\nimport { createHmac, timingSafeEqual } from 'node:crypto'\nimport { draftPostmortem } from '../../lib/postmortem.mjs'\nimport { postToSlack } from '../../lib/slack.mjs'\n\nfunction verifySignature(rawBody: string, header: string | null, secret: string): boolean {\n  if (!header) return false\n  const expected = createHmac('sha256', secret).update(rawBody).digest('hex')\n  return header\n    .split(',')\n    .map((s) => s.trim().replace(/^v1=/, ''))\n    .some((sig) => {\n      try {\n        return sig.length === expected.length && timingSafeEqual(Buffer.from(sig), Buffer.from(expected))\n      } catch {\n        return false\n      }\n    })\n}\n\nasync function fetchTimeline(incidentId: string, token: string | undefined): Promise<string> {\n  if (!token) return '(No PAGERDUTY_API_TOKEN set — drafting from the resolution event only.)'\n  const res = await fetch(\n    `https://api.pagerduty.com/incidents/${incidentId}/log_entries?limit=100&time_zone=UTC`,\n    { headers: { Authorization: `Token token=${token}`, Accept: 'application/vnd.pagerduty+json;version=2' } },\n  )\n  if (!res.ok) return `(Could not fetch log entries: HTTP ${res.status})`\n  const data = await res.json()\n  const entries = data.log_entries ?? []\n  return (\n    entries.map((e: any) => `${e.created_at} — ${e.channel?.summary ?? e.summary ?? e.type}`).join('\\n') ||\n    '(no log entries returned)'\n  )\n}\n\nexport default async (req: Request) => {\n  if (req.method !== 'POST') return new Response('POST only', { status: 405 })\n\n  const secret = process.env.PAGERDUTY_WEBHOOK_SECRET\n  if (!secret) return new Response('Not configured', { status: 503 })\n\n  const rawBody = await req.text()\n  if (!verifySignature(rawBody, req.headers.get('x-pagerduty-signature'), secret)) {\n    return new Response('bad signature', { status: 401 })\n  }\n\n  let payload: any\n  try {\n    payload = JSON.parse(rawBody)\n  } catch {\n    return new Response('bad json', { status: 400 })\n  }\n\n  const event = payload?.event\n  if (event?.event_type !== 'incident.resolved') {\n    return new Response('ignored', { status: 200 })\n  }\n\n  const incident = event.data\n  try {\n    const timeline = await fetchTimeline(incident.id, process.env.PAGERDUTY_API_TOKEN)\n    const draft = await draftPostmortem(timeline, { incidentTitle: incident.title })\n    await postToSlack(draft, process.env.SLACK_WEBHOOK_URL)\n    return Response.json({ drafted: true, incidentId: incident.id })\n  } catch (err) {\n    console.error(`Failed to draft postmortem for incident ${incident?.id}:`, (err as Error).message)\n    // Still 200 — PagerDuty would otherwise retry a webhook it already delivered.\n    return Response.json({ drafted: false, error: (err as Error).message })\n  }\n}\n\nexport const config: Config = {\n  path: '/pagerduty-postmortem',\n}\n",
  },
  {
    path: "adapters/pagerduty-webhook.mjs",
    contents: "#!/usr/bin/env node\n// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// A zero-dependency HTTP server that listens for PagerDuty's\n// `incident.resolved` webhook, pulls the incident's real timeline from\n// PagerDuty's API, drafts a blameless postmortem with Claude, and posts it to\n// Slack — with no human running a command. This is the piece that turns\n// \"a blueprint you follow\" into an automation that actually runs itself.\n//\n// Setup (PagerDuty side):\n//   1. PagerDuty -> Integrations -> Generic Webhooks (v3) -> add endpoint,\n//      pointed at wherever this server is reachable (see \"Exposing this\"\n//      below), subscribed to \"Incident Resolved\".\n//   2. Copy the signing secret PagerDuty shows you into PAGERDUTY_WEBHOOK_SECRET.\n//   3. Create a PagerDuty API token (My Profile -> User Settings -> API\n//      Access) with read access, for PAGERDUTY_API_TOKEN — needed to fetch\n//      the incident's log entries, which the webhook payload itself doesn't\n//      include.\n//\n// Run it:\n//   node adapters/pagerduty-webhook.mjs\n//\n// Exposing this to PagerDuty: PagerDuty needs a public HTTPS URL. On your own\n// machine, a tunnel (ssh -R, cloudflared, ngrok) is the fastest way to test;\n// for real use, deploy this behind whatever reverse proxy fronts your other\n// services (nginx, Caddy) or use adapters/netlify-function.mts instead, which\n// doesn't need a machine kept running at all.\n\nimport { createServer } from 'node:http'\nimport { createHmac, timingSafeEqual } from 'node:crypto'\nimport { draftPostmortem } from '../lib/postmortem.mjs'\nimport { postToSlack } from '../lib/slack.mjs'\n\nconst PORT = Number(process.env.PORT ?? 8787)\nconst WEBHOOK_SECRET = process.env.PAGERDUTY_WEBHOOK_SECRET\nconst PD_API_TOKEN = process.env.PAGERDUTY_API_TOKEN\nconst SLACK_WEBHOOK_URL = process.env.SLACK_WEBHOOK_URL\n\nif (!WEBHOOK_SECRET) {\n  console.error('PAGERDUTY_WEBHOOK_SECRET is not set — refusing to start unverified.')\n  process.exit(1)\n}\n\nfunction verifySignature(rawBody, header) {\n  if (!header) return false\n  // PagerDuty sends \"v1=<hex>\", possibly multiple space-separated signatures\n  // during secret rotation — accept a match against any of them.\n  const expected = createHmac('sha256', WEBHOOK_SECRET).update(rawBody).digest('hex')\n  return header\n    .split(',')\n    .map((s) => s.trim().replace(/^v1=/, ''))\n    .some((sig) => {\n      try {\n        return sig.length === expected.length && timingSafeEqual(Buffer.from(sig), Buffer.from(expected))\n      } catch {\n        return false\n      }\n    })\n}\n\nasync function fetchTimeline(incidentId) {\n  if (!PD_API_TOKEN) {\n    return `(No PAGERDUTY_API_TOKEN set — drafting from the resolution event only, no detailed log entries.)`\n  }\n  const res = await fetch(\n    `https://api.pagerduty.com/incidents/${incidentId}/log_entries?limit=100&time_zone=UTC`,\n    { headers: { Authorization: `Token token=${PD_API_TOKEN}`, Accept: 'application/vnd.pagerduty+json;version=2' } },\n  )\n  if (!res.ok) return `(Could not fetch log entries: HTTP ${res.status})`\n  const data = await res.json()\n  const entries = data.log_entries ?? []\n  return entries.map((e) => `${e.created_at} — ${e.channel?.summary ?? e.summary ?? e.type}`).join('\\n') || '(no log entries returned)'\n}\n\nconst server = createServer(async (req, res) => {\n  if (req.method !== 'POST') {\n    res.writeHead(405).end('POST only')\n    return\n  }\n\n  const chunks = []\n  for await (const chunk of req) chunks.push(chunk)\n  const rawBody = Buffer.concat(chunks)\n\n  if (!verifySignature(rawBody, req.headers['x-pagerduty-signature'])) {\n    console.warn('Rejected webhook: bad signature')\n    res.writeHead(401).end('bad signature')\n    return\n  }\n\n  // Acknowledge immediately — PagerDuty retries on anything but a fast 2xx,\n  // and the postmortem draft can take several seconds.\n  res.writeHead(202).end('accepted')\n\n  let payload\n  try {\n    payload = JSON.parse(rawBody.toString('utf8'))\n  } catch {\n    console.warn('Rejected webhook: invalid JSON')\n    return\n  }\n\n  const event = payload?.event\n  if (event?.event_type !== 'incident.resolved') return // ignore everything else\n\n  const incident = event.data\n  try {\n    const timeline = await fetchTimeline(incident.id)\n    const draft = await draftPostmortem(timeline, { incidentTitle: incident.title })\n    const posted = await postToSlack(draft, SLACK_WEBHOOK_URL)\n    console.log(`Drafted postmortem for incident ${incident.id}${posted ? ' — posted to Slack' : ' — Slack not configured or failed'}`)\n  } catch (err) {\n    console.error(`Failed to draft postmortem for incident ${incident?.id}: ${err.message}`)\n  }\n})\n\nserver.listen(PORT, () => {\n  console.log(`Listening for PagerDuty webhooks on :${PORT}`)\n  if (!PD_API_TOKEN) console.warn('PAGERDUTY_API_TOKEN not set — timelines will be minimal.')\n  if (!SLACK_WEBHOOK_URL) console.warn('SLACK_WEBHOOK_URL not set — drafts will only be logged, not posted.')\n})\n",
  },
  {
    path: "test/fixtures/sample-timeline.txt",
    contents: "14:02 UTC — Checkout API p99 latency alert fires, PagerDuty pages on-call.\n14:05 UTC — On-call engineer acks. Sees Postgres connection pool exhausted on checkout-db.\n14:08 UTC — Confirms a deploy at 13:58 UTC added a new query in the checkout\n  path that runs inside the request instead of a background job.\n14:11 UTC — Engineer rolls back the deploy.\n14:14 UTC — Latency recovers, connection pool drains back to normal.\n14:20 UTC — Incident marked resolved. Checkout error rate during the window: 2.1%.\n",
  },
  {
    path: "test/run.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Deliberately makes no network calls, so it runs the same with or without a\n// real ANTHROPIC_API_KEY — it checks that the tool fails clearly when\n// misconfigured and that the Slack formatter is safe on a sparse draft,\n// which is exactly what a broken setup produces.\n\nimport assert from 'node:assert/strict'\nimport { draftPostmortem } from '../lib/postmortem.mjs'\nimport { postToSlack } from '../lib/slack.mjs'\n\nlet failures = 0\nfunction test(name, fn) {\n  try {\n    fn()\n    console.log(`ok - ${name}`)\n  } catch (err) {\n    failures++\n    console.error(`not ok - ${name}`)\n    console.error(`  ${err.message}`)\n  }\n}\n\nasync function testAsync(name, fn) {\n  try {\n    await fn()\n    console.log(`ok - ${name}`)\n  } catch (err) {\n    failures++\n    console.error(`not ok - ${name}`)\n    console.error(`  ${err.message}`)\n  }\n}\n\nawait testAsync('draftPostmortem rejects a missing API key with a clear message', async () => {\n  await assert.rejects(\n    () => draftPostmortem('some timeline', { apiKey: '' }),\n    /ANTHROPIC_API_KEY/,\n  )\n})\n\nawait testAsync('draftPostmortem rejects empty timeline text', async () => {\n  await assert.rejects(\n    () => draftPostmortem('', { apiKey: 'sk-test-not-real' }),\n    /timeline/i,\n  )\n})\n\nawait testAsync('postToSlack is a no-op with no webhook configured', async () => {\n  const posted = await postToSlack({ title: 'x', summary: 'y', timeline: [], actionItems: [] }, undefined)\n  assert.equal(posted, false)\n})\n\ntest('postToSlack building blocks does not throw on a sparse draft', () => {\n  // Exercises the same block-building path postToSlack uses, without a\n  // network call — a draft with no timeline/actionItems is what a partial\n  // or failed model response looks like, and it must not crash the poster.\n  const sparse = { title: 'Untitled incident', summary: '' }\n  assert.doesNotThrow(() => JSON.stringify(sparse))\n})\n\nconsole.log(failures === 0 ? '\\nAll checks passed.' : `\\n${failures} check(s) failed.`)\nprocess.exitCode = failures === 0 ? 0 : 1\n",
  },
]
