// GENERATED FILE — DO NOT EDIT BY HAND.
//
// Produced by packages/supplier-po-sync/tools/embed-source.mjs from the real
// package source. Regenerate after changing the package:
//
//   node packages/supplier-po-sync/tools/embed-source.mjs
//
// This is the payload for the Supplier PO & Inventory Sync product: the
// complete, runnable source the buyer receives at checkout. It is embedded
// rather than read from disk so fulfilment cannot fail on a missing file.

export interface SourceFile {
  path: string
  contents: string
}

export const SUPPLIER_PO_SYNC_SOURCE: SourceFile[] = [
  {
    path: "README.md",
    contents: "# Supplier PO & Inventory Sync\n\nChecks your real Shopify stock against reorder thresholds you set, and — when\nsomething crosses one — generates and emails an actual purchase order (a real\nCSV attachment, a real email, to the actual supplier) automatically. Not a\nMake/Zapier blueprint you assemble from scratch — a working script that does\nthe whole job the moment it's configured.\n\n## How this differs from a low-stock *alert*\n\nIf you already own **MultiConnect: Shopify**, that product tells you a SKU\ncrossed its threshold. This product is the next step: it turns that moment\ninto a real purchase order in your supplier's inbox, with no human copying\nnumbers into a spreadsheet in between. They work well together, but this one\nalso runs completely standalone — it talks to Shopify directly and needs\nnothing else installed.\n\n## What it does, precisely\n\n1. On a schedule (default: every 6 hours — edit the cron in the workflow),\n   it reads `config.json`: the SKUs you're watching, each one's reorder\n   threshold and reorder quantity, and which supplier to email.\n2. It looks up each SKU's current stock directly from Shopify's Admin API.\n3. Anything at or below its threshold gets grouped by supplier — one\n   supplier with three low SKUs gets **one** combined PO, not three emails.\n4. For each supplier that needs one: builds a real purchase order (a\n   sequential PO number, a line-item CSV with quantities and costs, a plain\n   -text email body) and sends it via Resend.\n5. Optionally posts a one-line run summary to Slack either way — even \"ran,\n   nothing needed reordering\" — so you know the job is alive without\n   checking logs.\n\nIt does not place the order with the supplier's own system, negotiate\npricing, or guess a reorder quantity for you — you set the threshold and\nquantity per SKU once, and it acts on exactly that from then on.\n\n## Install (10 minutes)\n\n1. Copy `bin/`, `lib/`, and `config.example.json` into your repo — anywhere,\n   e.g. `tools/supplier-po-sync/`.\n2. Copy `config.example.json` to `config.json` in that same folder and edit\n   it: your real SKUs, thresholds, reorder quantities, and supplier emails.\n3. Copy `.github-workflow-template/supplier-po-sync.yml` to\n   `.github/workflows/supplier-po-sync.yml` (this exact path — GitHub only\n   runs workflows from there).\n4. If you put `bin/`/`lib/` somewhere other than `tools/supplier-po-sync/`,\n   edit the `working-directory:` line in the workflow to match.\n5. Add repo secrets (**Settings → Secrets and variables → Actions**):\n   - `SHOPIFY_STORE` — `your-shop.myshopify.com`\n   - `SHOPIFY_ACCESS_TOKEN` — from a Shopify custom app with `read_inventory`\n     + `read_products` scopes (Settings → Apps and sales channels → Develop\n     apps)\n   - `RESEND_API_KEY` — from resend.com. **Leave unset to dry-run**: POs are\n     still built and logged every run, just never sent — a safe way to watch\n     it for a few cycles before it emails a real supplier.\n   - `PO_FROM_EMAIL` — a verified sending address on your Resend domain\n   - `SLACK_WEBHOOK_URL` (optional) — Slack → Apps → Incoming Webhooks\n   - `STORE_NAME` (optional) — appears in the PO subject/body\n6. Commit and push. It runs on the schedule automatically, or trigger it\n   once immediately from the **Actions** tab (`workflow_dispatch`) to check\n   it actually works before waiting for the cron.\n\n## Testing without waiting for Actions\n\n```sh\nnpm install --omit=dev\ncp .env.example .env    # fill in SHOPIFY_STORE, SHOPIFY_ACCESS_TOKEN, etc.\ncp config.example.json config.json   # edit with your real SKUs/suppliers\nnode bin/sync.mjs\n```\n\nLeave `RESEND_API_KEY` blank in `.env` for your first run — you'll see\nexactly what PO each supplier *would* get, logged to the console, with\nnothing actually sent.\n\n```sh\nnpm test\n```\nRuns without any real API keys or network calls — checks the threshold\nlogic, PO/CSV builder, and config validation against known inputs, not a\nlive Shopify or Resend call.\n\n## On duplicate POs\n\nThis product has no database — each run is independent. If a SKU is still\nbelow threshold on the next run before you've restocked it, you'll get\nanother PO for it. In practice this is rarely a problem (suppliers are used\nto a follow-up PO on the same line, and most reorder cycles are longer than\nthis job's schedule), but if it matters to you: raise the schedule interval\nin the workflow (e.g. once daily instead of every 6 hours), or track sent POs\nyourself in a small file/database and skip a SKU that already has one open —\n`bin/sync.mjs` is plain, readable JavaScript specifically so you can extend\nit.\n\n## License\n\nSee `LICENSE.md`. One-time purchase, for your own use — run it on unlimited\nSKUs and suppliers, not for resale as a standalone product.\n",
  },
  {
    path: "LICENSE.md",
    contents: "# License\n\n**supplier-po-sync — perpetual single-purchase license**\n\n> This is a plain-language commercial license template. It has not been reviewed\n> by a lawyer. Have one look at it before you sell against it, and replace\n> `[SELLER]` and `[JURISDICTION]` with your details.\n\n## The short version\n\nYou bought it once. You own your copy forever. Run it against as many of your\n**own** stores, SKUs, and suppliers as you like. Do not resell it as a\nproduct of its own.\n\n## What you may do\n\n- Use the software for any purpose, commercial or personal, forever.\n- Run it on unlimited Shopify stores, SKUs, and suppliers that you own or\n  operate.\n- Use it in client work, and use the purchase orders it produces however you\n  like.\n- Modify the source freely. It is plain JavaScript with no build step precisely so\n  that you can.\n- Keep using it indefinitely. There is no license key, no activation, no expiry,\n  no phone-home, and nothing that stops working if [SELLER] does.\n- Keep and use any version you have received, forever, regardless of what happens\n  to later versions or to [SELLER].\n\n## What you may not do\n\n- Resell, relicense, sublicense or redistribute the software itself, in whole or\n  in substantial part, as a product or as part of a product whose value is\n  substantially this software.\n- Publish the source publicly, or include it in a public repository, package\n  registry, or template that others can obtain without buying it.\n- Remove or alter this license file or the attribution in the source headers.\n\nSelling the *output* — your own purchasing/ops process, consulting on\ninventory management — is explicitly allowed. Selling *the tool* is not.\n\n## Updates\n\nAny updates published within twelve months of your purchase are included at no\nextra cost. After that, your existing copy keeps working forever; new versions may\nrequire a new purchase. There is no subscription and no recurring charge of any\nkind.\n\n## Refunds\n\nBecause this is source code and delivery is immediate, a refund is available\nwithin 14 days of purchase if the software does not work as described. Run\n`npm test` before you ask — it takes a second and tells you whether the software\nis at fault.\n\n## Warranty and liability\n\nThe software is provided \"as is\", without warranty of any kind, express or\nimplied, including but not limited to the warranties of merchantability, fitness\nfor a particular purpose and non-infringement.\n\nIn no event shall [SELLER] be liable for any claim, damages or other liability,\nwhether in an action of contract, tort or otherwise, arising from, out of or in\nconnection with the software or its use.\n\nIn particular: this tool reads your store's own Shopify inventory data and\nsends real purchase-order emails to suppliers you configure. It does not\nplace orders through any supplier's own ordering system, negotiate pricing,\nor verify a supplier actually receives or acts on the email — you are\nresponsible for confirming delivery through your normal supplier\nrelationship, and for your own API keys, tokens, and their associated costs\nand access scopes.\n\n## Governing law\n\nThis license is governed by the laws of [JURISDICTION].\n\n---\n\nCopyright © 2026 [SELLER]. All rights reserved.\n\nThe source files each carry the same notice. Copyright in this software arises\nautomatically on creation and is not conditional on registration, on this notice,\nor on any filing — the notice exists to make ownership unambiguous and to travel\nwith a file that gets separated from this license.\n",
  },
  {
    path: "package.json",
    contents: "{\n  \"name\": \"supplier-po-sync\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Checks your Shopify stock against configured reorder thresholds and emails real purchase orders to your suppliers automatically — not a blueprint you wire yourself.\",\n  \"license\": \"SEE LICENSE IN LICENSE.md\",\n  \"type\": \"module\",\n  \"engines\": {\n    \"node\": \">=18\"\n  },\n  \"bin\": {\n    \"supplier-po-sync\": \"./bin/sync.mjs\"\n  },\n  \"main\": \"./lib/config.mjs\",\n  \"files\": [\n    \"bin\",\n    \"lib\",\n    \".github-workflow-template\",\n    \"config.example.json\",\n    \"README.md\",\n    \"LICENSE.md\"\n  ],\n  \"scripts\": {\n    \"sync\": \"node bin/sync.mjs\",\n    \"test\": \"node test/run.mjs\"\n  },\n  \"dependencies\": {},\n  \"devDependencies\": {}\n}\n",
  },
  {
    path: ".env.example",
    contents: "# Copy to .env for local testing. Inside GitHub Actions these are repo\n# secrets, not a file — see .github-workflow-template/supplier-po-sync.yml.\n\nSHOPIFY_STORE=your-shop.myshopify.com\nSHOPIFY_ACCESS_TOKEN=\n\n# Resend (https://resend.com) — used to actually send the PO email. Leave\n# unset to dry-run: POs are still built and logged, just not sent.\nRESEND_API_KEY=\nPO_FROM_EMAIL=purchasing@yourdomain.com\nSTORE_NAME=Your Store Name\n\n# Optional — posts a one-line run summary to Slack.\nSLACK_WEBHOOK_URL=\n\n# Optional — override the default ./config.json path.\nPO_CONFIG_PATH=\n",
  },
  {
    path: "config.example.json",
    contents: "[\n  {\n    \"sku\": \"WIDGET-BLUE-M\",\n    \"name\": \"Widget, Blue, Medium\",\n    \"threshold\": 20,\n    \"reorderQty\": 100,\n    \"unitCost\": 4.25,\n    \"supplierName\": \"Acme Supply Co.\",\n    \"supplierEmail\": \"orders@acmesupply.example.com\"\n  },\n  {\n    \"sku\": \"WIDGET-RED-L\",\n    \"name\": \"Widget, Red, Large\",\n    \"threshold\": 15,\n    \"reorderQty\": 75,\n    \"unitCost\": 4.5,\n    \"supplierName\": \"Acme Supply Co.\",\n    \"supplierEmail\": \"orders@acmesupply.example.com\"\n  },\n  {\n    \"sku\": \"GADGET-STD\",\n    \"name\": \"Gadget, Standard\",\n    \"threshold\": 10,\n    \"reorderQty\": 50,\n    \"supplierName\": \"Northline Parts\",\n    \"supplierEmail\": \"purchasing@northlineparts.example.com\"\n  }\n]\n",
  },
  {
    path: "lib/config.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nimport { readFileSync } from 'node:fs'\n\n/**\n * Loads and validates config.json — the list of SKUs this product watches,\n * their reorder threshold/quantity, and which supplier gets the PO. Kept as\n * plain JSON (not code) so a non-developer on the team can maintain it.\n *\n * @param {string} path\n * @returns {Array<{sku:string,name:string,threshold:number,reorderQty:number,\n *   unitCost?:number,supplierName:string,supplierEmail:string}>}\n */\nexport function loadConfig(path) {\n  let raw\n  try {\n    raw = readFileSync(path, 'utf8')\n  } catch (err) {\n    throw new Error(`Could not read config file at \"${path}\": ${err.message}`)\n  }\n\n  let parsed\n  try {\n    parsed = JSON.parse(raw)\n  } catch (err) {\n    throw new Error(`\"${path}\" is not valid JSON: ${err.message}`)\n  }\n\n  if (!Array.isArray(parsed)) throw new Error(`\"${path}\" must be a JSON array of SKU configs.`)\n\n  return parsed.map((line, i) => validateLine(line, i, path))\n}\n\nfunction validateLine(line, i, path) {\n  const required = ['sku', 'name', 'threshold', 'reorderQty', 'supplierName', 'supplierEmail']\n  for (const field of required) {\n    if (line[field] === undefined || line[field] === null || line[field] === '') {\n      throw new Error(`\"${path}\" entry ${i} is missing required field \"${field}\".`)\n    }\n  }\n  if (typeof line.threshold !== 'number' || line.threshold <= 0) {\n    throw new Error(`\"${path}\" entry ${i} (${line.sku}): \"threshold\" must be a positive number.`)\n  }\n  if (typeof line.reorderQty !== 'number' || line.reorderQty <= 0) {\n    throw new Error(`\"${path}\" entry ${i} (${line.sku}): \"reorderQty\" must be a positive number.`)\n  }\n  if (!/^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(line.supplierEmail)) {\n    throw new Error(`\"${path}\" entry ${i} (${line.sku}): \"supplierEmail\" doesn't look like a valid email.`)\n  }\n  return line\n}\n",
  },
  {
    path: "lib/mailer.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Sends the PO email via Resend's HTTP API (https://resend.com) — chosen\n// over SMTP specifically so this stays a plain `fetch` call with zero npm\n// dependencies, same as every other MultiNicheAI automation. Swap this file\n// for your own provider's API if you already use something else; every\n// transactional-email API looks roughly like this one.\n\nconst API_URL = 'https://api.resend.com/emails'\n\n/**\n * @param {object} email\n * @param {string} email.to\n * @param {string} email.subject\n * @param {string} email.text\n * @param {string} [email.fromName]\n * @param {string} [email.fromEmail]\n * @param {{filename:string, content:string}} [email.attachment]  content is\n *   the raw CSV text; this function base64-encodes it for the API.\n * @param {string} [apiKey]  defaults to RESEND_API_KEY\n * @returns {Promise<boolean>} true if sent, false if no key configured (a\n *   deliberate no-op, same pattern as the Slack webhook helper, so a dry\n *   run/test doesn't need a real account)\n */\nexport async function sendPoEmail(email, apiKey = process.env.RESEND_API_KEY) {\n  if (!apiKey) {\n    console.warn('RESEND_API_KEY is not set — skipping send (dry run).')\n    return false\n  }\n  if (!email.to) throw new Error('sendPoEmail requires email.to (the supplier\\'s address).')\n\n  const fromName = email.fromName ?? 'Purchasing'\n  const fromEmail = email.fromEmail ?? process.env.PO_FROM_EMAIL\n  if (!fromEmail) throw new Error('Set email.fromEmail or PO_FROM_EMAIL — Resend requires a verified sending domain.')\n\n  const body = {\n    from: `${fromName} <${fromEmail}>`,\n    to: [email.to],\n    subject: email.subject,\n    text: email.text,\n  }\n  if (email.attachment) {\n    body.attachments = [\n      { filename: email.attachment.filename, content: Buffer.from(email.attachment.content, 'utf8').toString('base64') },\n    ]\n  }\n\n  const res = await fetch(API_URL, {\n    method: 'POST',\n    headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },\n    body: JSON.stringify(body),\n    signal: AbortSignal.timeout(15_000),\n  })\n  if (!res.ok) {\n    const detail = await res.text().catch(() => '')\n    throw new Error(`Resend API returned HTTP ${res.status}: ${detail.slice(0, 300)}`)\n  }\n  return true\n}\n",
  },
  {
    path: "lib/po-builder.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Turns a supplier's grouped low-stock items into an actual purchase order:\n// a real CSV attachment (opens in Excel/Sheets/anything) plus the plain-text\n// body of the email that sends it. No PDF library, no templating engine —\n// a PO is a table and a few lines of text, and keeping this dependency-free\n// means it has nothing to break on an update.\n\nfunction csvEscape(value) {\n  const s = String(value ?? '')\n  return /[\",\\n]/.test(s) ? `\"${s.replace(/\"/g, '\"\"')}\"` : s\n}\n\n/**\n * @param {string} poNumber\n * @param {Array<{sku:string,name:string,available:number,reorderQty:number,unitCost?:number}>} items\n * @returns {string} CSV text\n */\nexport function buildPoCsv(poNumber, items) {\n  const rows = [['PO Number', 'SKU', 'Item', 'Currently in stock', 'Qty ordered', 'Unit cost', 'Line total']]\n  let total = 0\n  for (const item of items) {\n    const unitCost = Number(item.unitCost ?? 0)\n    const lineTotal = unitCost * item.reorderQty\n    total += lineTotal\n    rows.push([\n      poNumber,\n      item.sku,\n      item.name,\n      item.available,\n      item.reorderQty,\n      unitCost ? unitCost.toFixed(2) : '',\n      unitCost ? lineTotal.toFixed(2) : '',\n    ])\n  }\n  if (items.some((i) => i.unitCost)) {\n    rows.push(['', '', '', '', '', 'Total', total.toFixed(2)])\n  }\n  return rows.map((row) => row.map(csvEscape).join(',')).join('\\n') + '\\n'\n}\n\n/**\n * @param {string} poNumber\n * @param {string} supplierName\n * @param {Array<{sku:string,name:string,available:number,reorderQty:number,unitCost?:number}>} items\n * @param {string} [storeName]\n * @returns {string} plain-text email body\n */\nexport function buildPoEmailBody(poNumber, supplierName, items, storeName) {\n  const lines = [\n    `Purchase order ${poNumber}`,\n    storeName ? `From: ${storeName}` : null,\n    `To: ${supplierName}`,\n    '',\n    'The following items have reached their reorder point and need restocking:',\n    '',\n  ].filter(Boolean)\n\n  for (const item of items) {\n    lines.push(`  - ${item.name} (${item.sku}): ${item.reorderQty} units — currently ${item.available} in stock`)\n  }\n\n  lines.push('')\n  lines.push('Full line-item detail, including unit cost where configured, is attached as a CSV.')\n  lines.push('')\n  lines.push('This PO was generated automatically because stock crossed the reorder threshold configured for these items — please confirm receipt and expected delivery date.')\n\n  return lines.join('\\n')\n}\n\n/**\n * @param {number} n  a running counter (e.g. count of POs sent so far), used\n *   only to keep same-day PO numbers distinct — persistence is the caller's\n *   choice (a file, a database row, or just accept collisions are harmless\n *   since the CSV attachment is still fully correct either way).\n * @param {Date} [now]\n */\nexport function generatePoNumber(n = 0, now = new Date()) {\n  const y = now.getUTCFullYear()\n  const m = String(now.getUTCMonth() + 1).padStart(2, '0')\n  const d = String(now.getUTCDate()).padStart(2, '0')\n  const seq = String(n).padStart(3, '0')\n  return `PO-${y}${m}${d}-${seq}`\n}\n",
  },
  {
    path: "lib/shopify-client.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Reads current stock levels from Shopify's Admin REST API. Deliberately\n// narrow — this product only ever needs \"how many of this SKU are available\n// right now,\" nothing else about your store. A private/custom app token\n// scoped to read_inventory + read_products is all it needs; it never writes\n// to Shopify.\n\nconst API_VERSION = '2024-10'\n\nfunction storeUrl(store) {\n  const s = store ?? process.env.SHOPIFY_STORE\n  if (!s) throw new Error('SHOPIFY_STORE is not set (e.g. \"your-shop.myshopify.com\").')\n  return `https://${s}/admin/api/${API_VERSION}`\n}\n\nfunction authHeaders(token) {\n  const t = token ?? process.env.SHOPIFY_ACCESS_TOKEN\n  if (!t) throw new Error('SHOPIFY_ACCESS_TOKEN is not set — create a custom app with read_inventory + read_products scopes.')\n  return { 'X-Shopify-Access-Token': t, 'Content-Type': 'application/json' }\n}\n\n/**\n * Looks up current available stock for a list of SKUs. Shopify's REST API\n * has no \"get by SKU\" endpoint, so this does one product-search call per\n * SKU (fine at the scale a single-store PO run needs — dozens of SKUs, run\n * every few hours, not thousands run every minute).\n *\n * @param {string[]} skus\n * @param {object} [options]\n * @param {string} [options.store]\n * @param {string} [options.token]\n * @returns {Promise<Map<string, number>>} sku -> total available across all locations\n */\nexport async function getAvailableBySku(skus, options = {}) {\n  const base = storeUrl(options.store)\n  const headers = authHeaders(options.token)\n  const result = new Map()\n\n  for (const sku of skus) {\n    const res = await fetch(`${base}/products.json?fields=variants&limit=1&sku=${encodeURIComponent(sku)}`, { headers })\n    if (!res.ok) {\n      throw new Error(`Shopify API returned HTTP ${res.status} looking up SKU \"${sku}\"`)\n    }\n    const data = await res.json()\n    const variant = data?.products?.[0]?.variants?.find((v) => v.sku === sku)\n    // inventory_quantity on the variant is Shopify's own summed-across-locations\n    // total — no need to walk inventory_levels separately for this use case.\n    result.set(sku, variant ? Number(variant.inventory_quantity ?? 0) : 0)\n  }\n\n  return result\n}\n",
  },
  {
    path: "lib/slack.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Optional run summary, so you don't have to check your inbox/logs to know\n// the job ran and what it did. A no-op if SLACK_WEBHOOK_URL isn't set.\n\n/**\n * @param {Array<{poNumber:string, supplierName:string, itemCount:number, sent:boolean}>} results\n * @param {string|undefined} webhookUrl\n * @returns {Promise<boolean>}\n */\nexport async function postSummary(results, webhookUrl) {\n  if (!webhookUrl) return false\n\n  const text = results.length\n    ? `Supplier PO sync ran — ${results.length} PO(s) generated:\\n` +\n      results.map((r) => `• ${r.poNumber} — ${r.supplierName} (${r.itemCount} item${r.itemCount === 1 ? '' : 's'})${r.sent ? '' : ' — NOT sent (dry run / no email key)'}`).join('\\n')\n    : 'Supplier PO sync ran — nothing at or below its reorder threshold. No POs needed.'\n\n  try {\n    const res = await fetch(webhookUrl, {\n      method: 'POST',\n      headers: { 'content-type': 'application/json' },\n      body: JSON.stringify({ text }),\n      signal: AbortSignal.timeout(8000),\n    })\n    if (!res.ok) {\n      console.warn(`Slack webhook returned HTTP ${res.status}`)\n      return false\n    }\n    return true\n  } catch (error) {\n    console.warn(`Slack webhook failed: ${error?.message ?? error}`)\n    return false\n  }\n}\n",
  },
  {
    path: "lib/threshold.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// The one decision this whole product makes: given a current stock level and\n// a configured threshold, does this line need a purchase order? Kept as its\n// own tiny, pure module so the decision has a test independent of the\n// Shopify/email plumbing around it.\n\n/**\n * @param {number} available   current stock level from Shopify\n * @param {number} threshold   the configured reorder point for this SKU\n * @returns {boolean}\n */\nexport function needsReorder(available, threshold) {\n  if (!Number.isFinite(threshold) || threshold <= 0) return false\n  return Number.isFinite(available) && available <= threshold\n}\n\n/**\n * Groups the configured lines that currently need a reorder by supplier, so\n * one supplier with three low SKUs gets ONE combined PO/email, not three.\n *\n * @param {Array<{sku:string,name:string,available:number,threshold:number,\n *   reorderQty:number,unitCost?:number,supplierName:string,supplierEmail:string}>} lines\n *   `available` must already be attached to each line (see bin/sync.mjs).\n * @returns {Map<string, {supplierName:string, supplierEmail:string, items:Array}>}\n *   keyed by supplierEmail\n */\nexport function groupReorderBySupplier(lines) {\n  const bySupplier = new Map()\n  for (const line of lines) {\n    if (!needsReorder(line.available, line.threshold)) continue\n    const key = line.supplierEmail\n    if (!bySupplier.has(key)) {\n      bySupplier.set(key, { supplierName: line.supplierName, supplierEmail: line.supplierEmail, items: [] })\n    }\n    bySupplier.get(key).items.push(line)\n  }\n  return bySupplier\n}\n",
  },
  {
    path: "bin/sync.mjs",
    contents: "#!/usr/bin/env node\n// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\n// Entry point: reads config.json, checks each SKU's live Shopify stock,\n// groups anything at/below threshold by supplier, and sends one PO email\n// (with a CSV attached) per supplier that needs one. Safe to run as often\n// as you like — a SKU that isn't low doesn't get touched, and there's no\n// state to corrupt between runs (see README for why duplicate POs across\n// runs are a non-issue in practice, and how to avoid them if it matters to you).\n\nimport { loadConfig } from '../lib/config.mjs'\nimport { getAvailableBySku } from '../lib/shopify-client.mjs'\nimport { groupReorderBySupplier } from '../lib/threshold.mjs'\nimport { buildPoCsv, buildPoEmailBody, generatePoNumber } from '../lib/po-builder.mjs'\nimport { sendPoEmail } from '../lib/mailer.mjs'\nimport { postSummary } from '../lib/slack.mjs'\n\nasync function main() {\n  const configPath = process.env.PO_CONFIG_PATH ?? new URL('../config.json', import.meta.url).pathname\n  const config = loadConfig(configPath)\n\n  const available = await getAvailableBySku(config.map((c) => c.sku))\n  const withStock = config.map((c) => ({ ...c, available: available.get(c.sku) ?? 0 }))\n\n  const bySupplier = groupReorderBySupplier(withStock)\n  const results = []\n  let n = 0\n\n  for (const [, group] of bySupplier) {\n    n += 1\n    const poNumber = generatePoNumber(n)\n    const csv = buildPoCsv(poNumber, group.items)\n    const body = buildPoEmailBody(poNumber, group.supplierName, group.items, process.env.STORE_NAME)\n\n    const sent = await sendPoEmail({\n      to: group.supplierEmail,\n      subject: `Purchase order ${poNumber}${process.env.STORE_NAME ? ` — ${process.env.STORE_NAME}` : ''}`,\n      text: body,\n      fromName: process.env.STORE_NAME,\n      attachment: { filename: `${poNumber}.csv`, content: csv },\n    })\n\n    console.log(`${sent ? 'Sent' : 'Built (not sent)'} ${poNumber} to ${group.supplierName} — ${group.items.length} item(s)`)\n    results.push({ poNumber, supplierName: group.supplierName, itemCount: group.items.length, sent })\n  }\n\n  if (results.length === 0) {\n    console.log('Nothing at or below its reorder threshold. No POs needed.')\n  }\n\n  await postSummary(results, process.env.SLACK_WEBHOOK_URL)\n}\n\nmain().catch((err) => {\n  console.error(`supplier-po-sync failed: ${err.message}`)\n  process.exitCode = 1\n})\n",
  },
  {
    path: ".github-workflow-template/supplier-po-sync.yml",
    contents: "# Copy this file to .github/workflows/supplier-po-sync.yml in the repo you\n# run this from (this exact path — GitHub only runs workflows from\n# .github/workflows/). Also copy bin/, lib/, and your own config.json\n# somewhere in that repo (e.g. tools/supplier-po-sync/) and adjust the\n# `working-directory` below to match.\n#\n# Repo secrets required (Settings -> Secrets and variables -> Actions):\n#   SHOPIFY_STORE          your-shop.myshopify.com\n#   SHOPIFY_ACCESS_TOKEN   from a custom app with read_inventory + read_products\n#   RESEND_API_KEY         from resend.com — omit to dry-run (builds POs, logs\n#                          them, sends nothing)\n#   PO_FROM_EMAIL          a verified sending address on your Resend domain\n# Optional:\n#   SLACK_WEBHOOK_URL      posts a one-line run summary\n#   STORE_NAME             used in the PO subject/body\n\nname: Supplier PO sync\n\non:\n  schedule:\n    - cron: '0 */6 * * *' # every 6 hours — adjust to how often stock actually moves\n  workflow_dispatch: {} # lets you trigger it manually from the Actions tab too\n\njobs:\n  sync:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n\n      - name: Run supplier PO sync\n        working-directory: tools/supplier-po-sync # <- adjust to wherever you put lib/, bin/, config.json\n        env:\n          SHOPIFY_STORE: ${{ secrets.SHOPIFY_STORE }}\n          SHOPIFY_ACCESS_TOKEN: ${{ secrets.SHOPIFY_ACCESS_TOKEN }}\n          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}\n          PO_FROM_EMAIL: ${{ secrets.PO_FROM_EMAIL }}\n          STORE_NAME: ${{ secrets.STORE_NAME }}\n          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}\n        run: node bin/sync.mjs\n",
  },
  {
    path: "test/run.mjs",
    contents: "// Copyright (c) 2026 [SELLER]. All rights reserved.\n// Licensed to a single purchaser under the terms in LICENSE.md.\n// Redistribution or resale of this source, in whole or in part, is not permitted.\n\nimport assert from 'node:assert/strict'\nimport { writeFileSync, unlinkSync } from 'node:fs'\nimport { needsReorder, groupReorderBySupplier } from '../lib/threshold.mjs'\nimport { buildPoCsv, buildPoEmailBody, generatePoNumber } from '../lib/po-builder.mjs'\nimport { sendPoEmail } from '../lib/mailer.mjs'\nimport { loadConfig } from '../lib/config.mjs'\n\nlet failures = 0\nasync function test(name, fn) {\n  try {\n    await fn()\n    console.log(`ok - ${name}`)\n  } catch (err) {\n    failures++\n    console.error(`not ok - ${name}`)\n    console.error(`  ${err.message}`)\n  }\n}\n\nawait test('needsReorder: below threshold triggers, above does not', () => {\n  assert.equal(needsReorder(5, 20), true)\n  assert.equal(needsReorder(20, 20), true) // at threshold counts as needing reorder\n  assert.equal(needsReorder(21, 20), false)\n})\n\nawait test('needsReorder: ignores a zero/unset threshold rather than always firing', () => {\n  assert.equal(needsReorder(0, 0), false)\n  assert.equal(needsReorder(0, undefined), false)\n})\n\nawait test('groupReorderBySupplier: combines multiple low SKUs for the same supplier into one PO', () => {\n  const lines = [\n    { sku: 'A', name: 'A', available: 1, threshold: 10, reorderQty: 50, supplierName: 'Acme', supplierEmail: 'a@acme.example' },\n    { sku: 'B', name: 'B', available: 2, threshold: 10, reorderQty: 20, supplierName: 'Acme', supplierEmail: 'a@acme.example' },\n    { sku: 'C', name: 'C', available: 999, threshold: 10, reorderQty: 5, supplierName: 'Acme', supplierEmail: 'a@acme.example' },\n    { sku: 'D', name: 'D', available: 1, threshold: 10, reorderQty: 5, supplierName: 'Other', supplierEmail: 'x@other.example' },\n  ]\n  const grouped = groupReorderBySupplier(lines)\n  assert.equal(grouped.size, 2)\n  assert.equal(grouped.get('a@acme.example').items.length, 2) // C is fine, excluded\n  assert.equal(grouped.get('x@other.example').items.length, 1)\n})\n\nawait test('buildPoCsv: produces a header row, one row per item, and totals when cost is known', () => {\n  const csv = buildPoCsv('PO-TEST-001', [\n    { sku: 'A', name: 'Widget', available: 3, reorderQty: 10, unitCost: 2 },\n  ])\n  const rows = csv.trim().split('\\n')\n  assert.equal(rows.length, 3) // header + item + total\n  assert.match(rows[1], /PO-TEST-001,A,Widget,3,10,2\\.00,20\\.00/)\n  assert.match(rows[2], /Total,20\\.00/)\n})\n\nawait test('buildPoCsv: skips the total row when no item has a cost', () => {\n  const csv = buildPoCsv('PO-TEST-002', [{ sku: 'A', name: 'Widget', available: 3, reorderQty: 10 }])\n  assert.equal(csv.trim().split('\\n').length, 2) // header + item, no total row\n})\n\nawait test('buildPoCsv: quotes a field containing a comma', () => {\n  const csv = buildPoCsv('PO-TEST-003', [{ sku: 'A', name: 'Widget, Blue', available: 1, reorderQty: 1 }])\n  assert.match(csv, /\"Widget, Blue\"/)\n})\n\nawait test('buildPoEmailBody: names every item and the supplier', () => {\n  const body = buildPoEmailBody('PO-TEST-004', 'Acme Supply', [\n    { sku: 'A', name: 'Widget', available: 3, reorderQty: 10 },\n  ], 'My Store')\n  assert.match(body, /Acme Supply/)\n  assert.match(body, /Widget \\(A\\): 10 units/)\n  assert.match(body, /My Store/)\n})\n\nawait test('generatePoNumber: date-stamped and zero-padded', () => {\n  const n = generatePoNumber(3, new Date(Date.UTC(2026, 0, 5)))\n  assert.equal(n, 'PO-20260105-003')\n})\n\nawait test('sendPoEmail: is a no-op (dry run) with no API key configured', async () => {\n  const sent = await sendPoEmail({ to: 'a@b.example', subject: 'x', text: 'x' }, '')\n  assert.equal(sent, false)\n})\n\nawait test('sendPoEmail: rejects a missing recipient even with a key present', async () => {\n  await assert.rejects(() => sendPoEmail({ subject: 'x', text: 'x' }, 'fake-key'), /email\\.to/)\n})\n\nawait test('loadConfig: rejects a config entry missing a required field', () => {\n  const path = new URL('./tmp-bad-config.json', import.meta.url).pathname\n  writeFileSync(path, JSON.stringify([{ sku: 'A', name: 'A' }]))\n  try {\n    assert.throws(() => loadConfig(path), /missing required field/)\n  } finally {\n    unlinkSync(path)\n  }\n})\n\nawait test('loadConfig: rejects an invalid supplier email', () => {\n  const path = new URL('./tmp-bad-email.json', import.meta.url).pathname\n  writeFileSync(\n    path,\n    JSON.stringify([{ sku: 'A', name: 'A', threshold: 1, reorderQty: 1, supplierName: 'X', supplierEmail: 'not-an-email' }]),\n  )\n  try {\n    assert.throws(() => loadConfig(path), /valid email/)\n  } finally {\n    unlinkSync(path)\n  }\n})\n\nawait test('loadConfig: accepts the shipped config.example.json as-is', () => {\n  const path = new URL('../config.example.json', import.meta.url).pathname\n  const config = loadConfig(path)\n  assert.ok(config.length >= 1)\n})\n\nconsole.log(failures === 0 ? '\\nAll checks passed.' : `\\n${failures} check(s) failed.`)\nprocess.exitCode = failures === 0 ? 0 : 1\n",
  },
]
