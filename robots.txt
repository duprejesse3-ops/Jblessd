User-agent: *
Allow: /
Disallow: /admin
Disallow: /admin.html
# The browser code workspace and the apps it serves. These are the owner's own
# tools and drafts rather than storefront pages, so keeping them out of indexes
# stops them competing with the catalog for the domain's ranking. The /p/
# responses carry a matching X-Robots-Tag from netlify/functions/code-serve.mts,
# which is what actually removes an already-known URL — robots.txt only stops the
# crawl.
Disallow: /code
Disallow: /code.html
Disallow: /p/

User-agent: Bingbot
Allow: /

# Commercial SEO-tooling and bulk-scraping crawlers. These re-crawl the whole
# catalog aggressively, and because product and tool pages are rendered by an
# edge function, every page they take is a billed invocation. None of them send a
# visitor or influence any ranking that matters here, so the traffic is pure
# cost. Blocked by name rather than with a blanket rule so that search engines
# and assistant crawlers are unaffected — the store is deliberately readable by
# ChatGPT, Perplexity, Claude and Gemini (see the /llms.txt endpoint), because
# those citations send buyers.
User-agent: AhrefsBot
Disallow: /

User-agent: SemrushBot
Disallow: /

User-agent: MJ12bot
Disallow: /

User-agent: DotBot
Disallow: /

User-agent: DataForSeoBot
Disallow: /

User-agent: BLEXBot
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: PetalBot
Disallow: /

Sitemap: https://multinicheai.com/sitemap.xml
