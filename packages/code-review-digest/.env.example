# For local testing only — inside real GitHub Actions these are repo secrets,
# not a file. Copy to .env and fill in for local runs.

ANTHROPIC_API_KEY=
SLACK_WEBHOOK_URL=

# Only needed to run bin/digest.mjs outside of Actions.
GITHUB_TOKEN=
GITHUB_REPOSITORY=
