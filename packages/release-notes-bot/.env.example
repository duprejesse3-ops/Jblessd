# For local testing only — inside real GitHub Actions these are repo secrets.

ANTHROPIC_API_KEY=
SLACK_WEBHOOK_URL=

# Only needed to run bin/release-notes.mjs outside of Actions.
GITHUB_TOKEN=
GITHUB_REPOSITORY=

# Optional override — defaults to ./CHANGELOG.md
CHANGELOG_PATH=
